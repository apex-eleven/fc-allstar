/**
 * โครงสร้างข้อมูลสาธารณะของบัญชี (ส่วนที่ไม่ผูกกับ Firebase)
 *
 * แยกออกมาจาก services/firebase/profiles.ts เพราะฝั่งเซิร์ฟเวอร์ (Cloud Functions)
 * ต้องใช้ชนิดนี้ด้วย แต่ import ไฟล์นั้นไม่ได้ — มันดึง Firebase ฝั่งเบราว์เซอร์
 * ที่อ่าน import.meta.env ตามมา ซึ่งคอมไพล์ในโหมด Node ไม่ผ่าน
 */

/**
 * ตัวจริงหนึ่งช่องที่เปิดให้คนอื่นดูได้
 * เก็บแค่ "ใครอยู่ช่องไหน + ตีบวกเท่าไหร่" ส่วนรายละเอียดนักเตะอ่านจาก data/players.ts
 * ของเครื่องคนดูเอง จึงไม่ต้องส่งข้อมูลก้อนใหญ่ข้ามเครื่อง
 */
export interface PublicSquadSlot {
  slotId: string;
  playerId: string;
  /** เลเวลการ์ด (1 = +0) */
  level: number;
}
