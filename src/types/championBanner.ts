/**
 * แบนเนอร์อันดับ 1 ในตารางอันดับ (config/championBanner) — ADMIN → แบนเนอร์อันดับ 1
 *
 * รูปพื้นหลังของแถวอันดับ 1 พร้อมตัวคุมความมืด เพื่อให้ตัวเลขอันดับ ชื่อทีม และดาว
 * ยังอ่านออกอยู่เสมอ — ข้อมูลในตารางสำคัญกว่าความสวยของรูป
 */
export interface ChampionBannerConfig {
  /** false = แถวอันดับ 1 กลับไปใช้แถบทองแบบเดิม */
  enabled: boolean;
  /** พาธรูปใน public/ (แนะนำ) หรือ data URL ที่อัปโหลดเข้ามา */
  image: string;
  /**
   * ความมืดของฝ้าที่คลุมทับรูป 0–0.95
   * ยิ่งมากยิ่งอ่านข้อมูลง่าย แต่รูปจะจางลง — ค่าเริ่มต้นเลือกไว้ให้อ่านออกสบายก่อน
   */
  overlay: number;
  /** ตำแหน่งแนวตั้งของรูปในแถบ 0–100% (แถวเตี้ยกว่ารูปมาก จึงต้องเลือกว่าจะโชว์ช่วงไหน) */
  focusY: number;
}
