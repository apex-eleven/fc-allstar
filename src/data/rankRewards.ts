/**
 * ═══════════════════════════════════════════════════════════════
 *  รางวัลการ์ดของตารางอันดับ (อันดับ 1–10 ปลายซีซัน)
 *  ไฟล์นี้คือที่ที่ "เจ้าของโปรเจค" ตั้งค่าเองได้ทั้งหมด
 * ═══════════════════════════════════════════════════════════════
 *
 * มีสองวิธีเปลี่ยนการ์ดรางวัล เลือกอย่างใดอย่างหนึ่ง:
 *
 *   วิธีที่ 1 — แก้ในไฟล์นี้ (ต้อง deploy ใหม่)
 *     แก้ id ในรายการ DEFAULT_RANK_REWARDS ข้างล่าง (ดู id ได้ที่ src/data/roster.ts)
 *
 *   วิธีที่ 2 — แก้ในเกม (มีผลทันที ไม่ต้อง deploy)
 *     ล็อกอินด้วยไอดีที่อยู่ใน OWNER_USERNAMES → เข้าหน้า Leaderboard
 *     → กดปุ่ม "ตั้งค่ารางวัล" เหนือตาราง → เลือกการ์ดของแต่ละอันดับ → บันทึก
 *     ค่าที่บันทึกจะถูกเก็บบน Firestore (config/rankRewards) และผู้เล่นทุกคนเห็นเหมือนกัน
 *     ถ้ายังไม่เคยบันทึก เกมจะใช้ค่าในไฟล์นี้เป็นค่าเริ่มต้น
 *
 * หมายเหตุสำหรับวิธีที่ 2: ต้องเปิดสิทธิ์เขียนใน firestore.rules ให้ uid ของเจ้าของก่อน
 * (ดูคำอธิบายในไฟล์ firestore.rules — ปุ่มตั้งค่ารางวัลจะโชว์ uid ของคุณให้ก๊อปไปใส่)
 */
import type { CardPack } from '@/types/card';

/**
 * ไอดีผู้เล่น (ที่ใช้ล็อกอิน) ของเจ้าของโปรเจค — ใส่ได้หลายคน
 * มีเฉพาะคนในรายการนี้ที่เห็นปุ่ม "ตั้งค่ารางวัล"
 * ตัวพิมพ์เล็ก-ใหญ่ไม่มีผล
 */
export const OWNER_USERNAMES: string[] = ['jameskrub'];

/**
 * จำนวนอันดับที่ได้รางวัลการ์ด (ค่าเริ่มต้น)
 * เจ้าของโปรเจคเปลี่ยนจำนวนได้จากหน้า ADMIN → รางวัลอันดับ โดยไม่ต้อง deploy
 */
export const REWARD_RANKS = 10;

/** ช่วงจำนวนรางวัลที่ตั้งได้ */
export const REWARD_RANKS_RANGE = { min: 1, max: 20 } as const;

/**
 * การ์ดรางวัลของอันดับ 1 ถึงอันดับนี้ จะถูกกันออกจากร้านแลกนักเตะด้วยแต้ม
 *
 * เหตุผล: รางวัลสามอันดับแรกควรเป็นของที่ "หาไม่ได้จากที่อื่น" ถึงจะมีค่าพอ
 * ให้คนแย่งกันขึ้นอันดับ ถ้าเดินไปกดแลกด้วยแต้มได้เหมือนกัน รางวัลก็ไม่มีความหมาย
 * (รางวัลอันดับ 4 ลงไปยังเข้าร้านได้ตามปกติ)
 */
export const SHOP_PROTECTED_RANKS = 3;

/**
 * การ์ดรางวัลเริ่มต้นของอันดับ 1 → 10
 * ตำแหน่งที่ 0 ในรายการคือรางวัลอันดับ 1
 */
export const DEFAULT_RANK_REWARDS: string[] = [
  'p061', // อันดับ 1
  'p063', // อันดับ 2
  'p065', // อันดับ 3
  'p066', // อันดับ 4
  'p067', // อันดับ 5
  'p068', // อันดับ 6
  'p064', // อันดับ 7
  'p062', // อันดับ 8
  'p036', // อันดับ 9
  'p027', // อันดับ 10
];

/** จำนวนการ์ดที่คนไม่ติดอันดับ 1–10 ได้รับทุกคน */
export const CONSOLATION_CARD_COUNT = 10;

/**
 * แพ็คสุ่มของคนที่ไม่ติดอันดับ — ใช้ระบบเปิดซองชุดเดียวกับร้าน (services/cardPack.ts)
 * ปรับ odds ตรงนี้ได้เลยถ้าอยากให้ใจดีขึ้นหรือหินขึ้น
 */
export const CONSOLATION_PACK: CardPack = {
  id: 'pack-season-consolation',
  name: 'Season Reward Pack',
  tier: 'special',
  price: 0,
  cardCount: CONSOLATION_CARD_COUNT,
  odds: { common: 84, rare: 10, epic: 4, legendary: 2, mythical: 0 },
  description: 'แพ็คสุ่มปลายซีซันของผู้เล่นที่ไม่ติดอันดับ 1–10',
};
