# APEX ELEVEN — ดาวปิดสนิท (ขั้นที่ 1 เสร็จสมบูรณ์)

## ทุกอย่างพร้อมแล้ว — แต่ต้องเปิดตามลำดับ ห้ามข้าม

ทั้งหมดถูกออกแบบให้ **เปิดทีละขั้นได้โดยเกมไม่สะดุด** มีสวิตช์ 2 ตัว
ตอนนี้ทั้งคู่ยัง "ปิด" อยู่ เกมจึงทำงานเหมือนเดิมทุกประการแม้อัปโค้ดขึ้นไปแล้ว

| สวิตช์ | อยู่ที่ | ค่าตอนนี้ |
|---|---|---|
| `VITE_SERVER_AUTHORITY` | Environment Variable บน Vercel | ยังไม่ได้ตั้ง = ปิด |
| `serverOwnsPoints()` | `firestore.rules` | `false` = ปิด |

### ลำดับการเปิด (ทำตามนี้เป๊ะ ๆ)

**1. อัปเป็นแพลน Blaze** — Cloud Functions ใช้กับ Spark ไม่ได้
โควตาฟรี 2 ล้านครั้ง/เดือนยังอยู่ · ตั้ง `maxInstances: 10` กันบานปลายไว้แล้ว
แนะนำตั้งงบเตือนที่ Console → ⚙️ Usage and billing → Budgets & alerts

**2. deploy functions**
```bash
cd functions && npm install
firebase deploy --only functions
```
ครั้งแรกจะถามให้เปิด API หลายตัว ตอบ yes · ใช้เวลาราว 2–3 นาที

**3. push โค้ดขึ้น GitHub** ให้ Vercel deploy (ยังไม่ต้องตั้ง env)

**4. เปิดสวิตช์ตัวแรก** — Vercel → Settings → Environment Variables
เพิ่ม `VITE_SERVER_AUTHORITY` = `1` แล้ว **Redeploy**

**5. ทดสอบให้ชัวร์ก่อนไปต่อ** — ลงแข่ง 1 นัด · เข้าร่วมลีก · รอรอบลีกเดิน
ถ้าทุกอย่างปกติค่อยไปข้อ 6 (ถ้าพัง แค่ลบ env ตัวนั้นแล้ว redeploy = กลับสภาพเดิมทันที)

**6. เปิดสวิตช์ตัวที่สอง** — แก้ `firestore.rules`
```
function serverOwnsPoints() {
  return true;      // ← เปลี่ยนจาก false
}
```
แล้ว `firebase deploy --only firestore:rules`

จบข้อ 6 = **ดาวปิดสนิท** แก้อะไรในเบราว์เซอร์ก็ไม่ขยับแม้แต่ดวงเดียว

## รอบนี้ทำอะไรไปบ้าง

### ฝั่งเซิร์ฟเวอร์ — ปิดครบทุกทางที่ดาวขยับ
| ฟังก์ชัน | ปิดช่องอะไร |
|---|---|
| `playMatch` | ลงแข่งเอง — เซิร์ฟเวอร์คำนวณค่าพลังจากคลังการ์ดจริง สุ่มผล บวกดาวเอง **และอัปเดตสถิติของอีกฝ่ายพร้อมกัน** |
| `syncLeague` | ลีกประจำวัน — เดินรอบที่ค้างอยู่ทั้งหมดตามนาฬิกาเซิร์ฟเวอร์ |
| `setLeagueJoined` | เข้าร่วม/ออกจากลีก — สถานะลีกเป็นของเซิร์ฟเวอร์ |

**ทางที่ดาวขยับได้มี 3 ทาง และปิดครบแล้วทั้งหมด:**
ลงแข่งเอง · โดนคนอื่นท้าตอนไม่อยู่ · ลีกเดินรอบ

จุดที่แก้เพิ่มจากรอบก่อน: `playMatch` อัปเดตสถิติของ **ทั้งสองฝ่าย** ในคำขอเดียว
ใบในกล่องผลการแข่งจึงเหลือหน้าที่แค่ "บอกให้รู้" ฝั่งผู้รับไม่ต้องบวกดาวเองอีก
(เดิมฝั่งผู้รับเป็นคนบวก = ยังเป็นช่องอยู่)

### ฝั่งหน้าเว็บ
- `kickoff` ขอผลจากเซิร์ฟเวอร์แล้วเอามา "เล่นเทป" ถ่ายทอดสด — ภาพที่ผู้เล่นเห็นเหมือนเดิมทุกอย่าง
  ดาวถูกบวกจบไปแล้วตั้งแต่ก่อนภาพแรกขึ้นจอ
- ดาวที่แสดงบนหน้าจอดึงจากโปรไฟล์ของตัวเองบนเซิร์ฟเวอร์เสมอ ไม่บวกเอง
- ต่อเซิร์ฟเวอร์ไม่ได้ = ลงแข่งไม่ได้ พร้อมข้อความบอกเหตุผล
  (ไม่ถอยไปคิดผลในเครื่อง เพราะนั่นเท่ากับเปิดช่องกลับมา)
- เซฟที่เขียนขึ้นคลาวด์ตัด `record` / `league` / `recentRivals` / `lastMatchAt` ออกแล้ว

### กฎ Firestore
- สวิตช์ `serverOwnsPoints()` — เปิดแล้วเครื่องผู้เล่นแก้ดาวและสถิติแพ้-ชนะไม่ได้เลย
- `accounts` เขียนได้ แต่ฟิลด์ของเซิร์ฟเวอร์ต้องไม่ขยับ
- `npm run check:rules` เพิ่มเทสโหมดล็อกแล้ว: เพิ่มดาว/ลดดาว/แก้สถิติ = ต้องถูกปฏิเสธทั้งหมด
  ส่วนแก้ชื่อทีมโดยไม่แตะดาว = ต้องผ่านปกติ

## เหลืออะไรอีก (ขั้นที่ 2)

ดาวปิดแล้ว แต่ `state.cards` / `state.coins` ยังเขียนจากเครื่องได้
คนตั้งใจโกงยังเสกการ์ด Mythical ครบทีมแล้วชนะรัวแบบ "ถูกกติกา" ได้
— ได้ดาวช้าเท่าคนอื่น (คูลดาวน์ 15 วิ/นัด · 30 นาที/คู่แข่งคนเดิม) แต่ชนะเกือบทุกนัด

ขั้นที่ 2 = ย้าย เปิดซอง / แลกแต้ม / ย่อยการ์ด / ตีบวก ไปเซิร์ฟเวอร์
แล้วล็อก `accounts` ให้เครื่องผู้เล่นเขียนได้แค่ชื่อทีม รูปโปรไฟล์ และการจัดตัว

## เทส
`npm test` 37 ข้อ · `npm run smoke` ผ่าน · `functions` คอมไพล์ผ่าน
`npm run check:rules` — **รบกวนรันฝั่งคุณก่อน publish** ผมรันไม่ได้ในเครื่องผม (โหลดตัวจำลองไม่ได้)

---

# APEX ELEVEN — ย้ายไปเซิร์ฟเวอร์จริง (ขั้นที่ 1 จาก 3)

## สถานะ: ยังไม่ปิดช่อง — นี่คือฐานราก อ่านให้จบก่อน deploy

รอบนี้ได้ **ฝั่งเซิร์ฟเวอร์ที่ตัดสินผลแมตช์เองได้จริง** (คอมไพล์ผ่าน พร้อม deploy)
แต่ **ยังไม่มีอะไรบังคับให้เครื่องผู้เล่นต้องใช้มัน** ต้องทำอีก 3 อย่างถึงจะกันได้จริง

**อย่าเพิ่งล็อก firestore.rules ในรอบนี้** — ลีกยังแจกดาวเองที่เครื่องผู้เล่นอยู่
ถ้าล็อกตอนนี้ ลีกจะเขียนเซฟไม่ผ่านแล้วเกมพังทั้งระบบ

## สิ่งที่ทำเสร็จแล้ว

### โฟลเดอร์ `functions/` — Cloud Functions
- ใช้ **กติกาชุดเดียวกับหน้าเว็บ** ผ่าน path alias `@/*` → `../src`
  ไม่ได้ก๊อปกติกามาไว้อีกชุด แก้สมดุลที่เดียวตรงกันทั้งสองฝั่ง
- `npm --prefix functions run build` คอมไพล์ผ่าน ทดสอบ import แล้วเรียกใช้ได้จริง
- `firebase.json` ต่อสายไว้แล้ว — `firebase deploy --only functions` ใช้ได้เลย

### `playMatch` — เซิร์ฟเวอร์ตัดสินผลแมตช์
เครื่องผู้เล่นส่งมาได้อย่างเดียวคือ *"อยากเจอ uid ไหน"*
ทุกตัวเลขที่มีผลกับแพ้ชนะถูกอ่านจากฐานข้อมูลฝั่งเซิร์ฟเวอร์เองทั้งหมด

ด่านที่ต้องผ่านก่อนได้ดาว:
1. ล็อกอินจริง
2. จัดตัวครบ 11 คน โดยการ์ดทุกใบต้องมีอยู่จริงในคลัง — **อ้างชื่อการ์ดมั่วไม่ได้**
3. คูลดาวน์ 15 วินาทีต่อนัด
4. คูลดาวน์ 30 นาทีต่อคู่แข่งคนเดิม (กันนัดกันผลัดแพ้ชนะปั้มดาว)
5. ท้าตัวเองไม่ได้

ใบรายงานผลไปหาอีกฝ่ายก็เขียนจากเซิร์ฟเวอร์แล้ว — เดิมเครื่องผู้ท้าเป็นคนเขียน
แปลว่าปลอมสกอร์ให้คู่แข่งแพ้ได้ ตอนนี้ทำไม่ได้อีก

### `ping` — เช็คว่า deploy สำเร็จและหน้าเว็บเรียกฟังก์ชันได้จริง

## เหลืออีก 3 อย่าง (รอบหน้า)

1. **`syncLeague`** — ย้ายการคิดรอบลีกประจำวันมาที่เซิร์ฟเวอร์
   ตอนนี้ลีกยังคิดผลและแจกดาวเองที่เครื่องผู้เล่น ซึ่งเป็นช่องที่ใหญ่กว่า Matchmaking อีก
2. **ต่อสายหน้าเว็บ** ให้ `kickoff` เรียก `playMatch` แทน `simulateMatch` ในเครื่อง
   แล้วเอาผลที่เซิร์ฟเวอร์ส่งกลับมา "เล่นเทป" ถ่ายทอดสด
   พร้อมตัด `record` / `recentRivals` / `lastMatchAt` ออกจากสิ่งที่เครื่องผู้เล่นเขียน
3. **ล็อก `firestore.rules`** ห้ามเครื่องผู้เล่นแก้ดาวเด็ดขาด
   (`profiles.points` และ `accounts.state.record` ต้องเท่าเดิมเสมอในทุกคำขอจากเครื่องผู้เล่น)
   Admin SDK ที่ฟังก์ชันใช้ข้ามกฎอยู่แล้ว จึงเขียนได้ฝ่ายเดียว

ทำครบ 3 ข้อนี้ = **ดาวปิดสนิท** แก้อะไรในเบราว์เซอร์ก็ไม่ขยับ

## แล้วเหรียญกับการ์ดล่ะ (ขั้นที่ 2)

ดาวปิดแล้วแต่ `state.cards` / `state.coins` ยังเขียนจากเครื่องผู้เล่นได้อยู่
คนที่ตั้งใจโกงยังเสกการ์ด Mythical ครบทีมให้ตัวเองได้ แล้วชนะรัวแบบ "ถูกกติกา"
(ได้ดาวช้าเท่าคนอื่น แต่ชนะเกือบทุกนัด)

ขั้นที่ 2 = ย้าย เปิดซอง / แลกแต้ม / ย่อยการ์ด / ตีบวก ไปเซิร์ฟเวอร์ทั้งหมด
แล้วล็อก `accounts` ให้เครื่องผู้เล่นเขียนได้แค่ชื่อทีม รูปโปรไฟล์ และการจัดตัว

## ก่อน deploy ต้องรู้

- **ต้องอัปเป็นแพลน Blaze** (ผูกบัตร) — Cloud Functions ใช้กับ Spark ไม่ได้
  โควตาฟรี 2 ล้านครั้ง/เดือนยังอยู่ เกม 250 คนไม่มีทางถึง
  ตั้ง `maxInstances: 10` ไว้แล้วกันค่าใช้จ่ายบานปลาย และควรตั้งงบเตือนใน Console ด้วย
- **ภูมิภาค `asia-southeast1`** (สิงคโปร์) ใกล้ไทยที่สุด
- ครั้งแรกจะช้ากว่าปกติราว 1–2 วินาที (cold start) หลังจากนั้นเร็ว

## เทส
`npm test` 37 ข้อ (เพิ่มเรื่องค่าพลังที่เซิร์ฟเวอร์คำนวณเอง, การ์ดปลอม, คูลดาวน์รายนัด)

---

# APEX ELEVEN — อัปเดตด่วน: กันโปรแกรมโกงยัดดาว

## ปัญหา
มีคนได้ 4,000+ ดาวในไม่ถึง 5 นาที ซึ่งเป็นไปไม่ได้จากการเล่นจริง
(ชนะ 1 นัด = 1 ดาว · หนึ่งนัดใช้เวลาราว 90 วินาที)
แปลว่าเขาเขียนค่าลง Firestore ตรง ๆ ไม่ได้ผ่านการเล่น

**สาเหตุ:** กฎเดิมยอมรับดาวเท่าไหร่ก็ได้ขอแค่ `points >= 0`
ใครเปิด DevTools ก็ยิงคำขอเขียนโปรไฟล์ตัวเองด้วยเลขอะไรก็ได้

## วิธีแก้: เพดานอัตราการโตของดาว (บังคับที่เซิร์ฟเวอร์)

เซิร์ฟเวอร์ไม่มีทางรู้ว่าแมตช์เกิดขึ้นจริงไหม (เกมจำลองผลที่เครื่องผู้เล่น)
แต่คุม **"ความเร็ว"** ที่ดาวโตได้ ให้อยู่ในระดับที่เล่นจริงเท่านั้นถึงจะทำได้

```
ดาวที่เพิ่มได้ในหนึ่งคำขอ ≤ 60 + (นาทีที่ผ่านไปตั้งแต่ดาวขยับครั้งล่าสุด)
```

- **60 แรก** เผื่อให้คนที่หายไปนานแล้วกลับมา — ลีกจะคิดผลย้อนหลังทีเดียวหลายรอบ
- **ตัวบวกรายนาที** ทำให้คนที่ยิงคำขอรัว ๆ ได้ดาวเพิ่มแทบไม่ขึ้น
- เวลาถูกประทับจาก **นาฬิกาเซิร์ฟเวอร์** (`pointsUpdatedAt`) กฎบังคับว่าต้องเท่ากับ
  เวลาที่เซิร์ฟเวอร์รับคำขอเสมอ ปลอมเวลาจากเครื่องตัวเองไม่ได้
- ดาว **ลดลง** ได้เสมอ (แพ้ / แอดมินรีเซ็ต) และแก้ชื่อทีม/รูป/ตัวจริงโดยไม่แตะดาวก็ผ่านปกติ
- สร้างโปรไฟล์ครั้งแรกต้องเริ่มจากดาวไม่เกิน 60 — เปิดบัญชีใหม่มาพร้อมหลักพันไม่ได้

ผลลัพธ์: ยัด 4,000 ดาวทีเดียว → **ถูกปฏิเสธที่เซิร์ฟเวอร์** ต่อให้แก้โค้ดหน้าเว็บก็ไม่ผ่าน
คนที่พยายามยัดจะกลายเป็นว่าโปรไฟล์เขียนไม่ผ่านเลย ดาวค้างอยู่ที่เดิม

## ตรวจกฎก่อน deploy ได้แล้ว
เพิ่มสคริปต์ `npm run check:rules` — ยิงคำขอปลอมใส่กฎจริงในตัวจำลองบนเครื่อง
แล้วบอกว่ากฎอนุญาต/ปฏิเสธตรงตามที่ตั้งใจไหม (ไม่แตะฐานข้อมูลจริง)

ครั้งแรกต้องติดตั้งก่อน (ต้องมี Java):
```
npm i -D --legacy-peer-deps firebase-tools @firebase/rules-unit-testing
npm run check:rules
```
ครอบคลุม: ยัดดาว · ปลอมเวลา · เขียนโปรไฟล์คนอื่น · สิทธิ์แอดมิน · เพดานของขวัญ

## ต้องทำหลังอัปเดต
1. `firebase deploy --only firestore:rules` (หรือวางในหน้า Rules แล้ว Publish)
2. push โค้ดขึ้น GitHub ให้ Vercel deploy — **สำคัญ** เพราะฝั่งเกมต้องเริ่มส่ง
   `pointsUpdatedAt` มาด้วย ถ้าอัปแต่กฎไม่อัปโค้ด ทุกคนจะเขียนดาวไม่ผ่าน
3. รีเซ็ตดาวคนที่โกงจากหน้า ADMIN

---

# APEX ELEVEN — อัปเดต: เมนู ADMIN (รอบที่ 1)

## เปิดใช้ยังไง (ต้องทำ 2 อย่างก่อน)

**1. ใส่ไอดีของคุณใน `src/data/rankRewards.ts`**
```ts
export const OWNER_USERNAMES: string[] = ['owner', 'admin'];  // ← ใส่ไอดีที่คุณใช้ล็อกอิน
```
เมนู **ADMIN** จะโผล่ใน sidebar เฉพาะไอดีในรายการนี้ คนอื่นไม่เห็นและเปิด `/admin` เองก็ไม่ได้

**2. ใส่ uid ของคุณใน `firestore.rules`**
```
function isProjectOwner() {
  return isSignedIn() && request.auth.uid in ['ใส่-uid-ของเจ้าของที่นี่'];
}
```
uid ดูได้จากหัวหน้า ADMIN ในเกม (บรรทัด "uid ของคุณ: …") — ก๊อปมาวางแล้ว deploy:
```
firebase deploy --only firestore:rules
```
ข้อนี้สำคัญ: ถ้าไม่ทำ เมนูจะเปิดได้แต่ **กดบันทึกอะไรไม่ได้เลย** (Firestore ปฏิเสธที่เซิร์ฟเวอร์)
นี่คือด่านความปลอดภัยจริง — ต่อให้ใครแก้โค้ดหน้าเว็บให้ตัวเองเป็นแอดมิน ก็ยังเขียนอะไรไม่ได้

## มีอะไรบ้างในรอบนี้

### 1. เสกของ — เหรียญ / แต้มแลกนักเตะ / แต้มตีบวก / การ์ด
ส่งได้ 3 แบบ:
- **ตัวเอง** — เข้าบัญชีทันที
- **เลือกคน** — ค้นหาจากชื่อทีม/ชื่อผู้จัดการ แล้วส่งให้คนนั้น
- **ทุกคน** — หย่อนให้ทุกบัญชีในตารางอันดับรวดเดียว (ใช้ตอนแจกของชดเชย/อีเวนต์)

การ์ดเลือกได้หลายใบพร้อมกัน (สูงสุด 30 ใบต่อครั้ง) ใบเดิมซ้ำได้ ใส่ข้อความถึงผู้รับได้ด้วย

**ทำงานยังไง:** แอดมินเขียนบัญชีคนอื่นไม่ได้ (กฎล็อกไว้ และไม่ควรปลดล็อก) จึงใช้
**กล่องของขวัญ** — แอดมินหย่อนใบสั่งลง `gifts/{uid}` พอเจ้าของเปิดเกมของก็เข้าทันที
พร้อมแจ้งเตือนมุมขวาบน แล้วใบถูกลบทิ้ง ไม่ได้ซ้ำ ใช้โครงเดียวกับกล่องผลการแข่งที่มีอยู่แล้ว

### 2. รีเซ็ตดาวทุกคน
เลือกได้ว่าล้างหมด / เหลือ 30% / เหลือ 50% และติ๊กขึ้นซีซันใหม่ให้ทุกคนพร้อมกันได้
มีขั้นยืนยันก่อนลงมือเสมอ

**ทำงานสองชั้น:** เขียนทับดาวใน `profiles` ของทุกคน → **ตารางอันดับเป็นศูนย์ทันทีทั้งกระดาน**
พร้อมกับประทับเวลาคำสั่งไว้ที่ `config/ladder` → ดาวในบัญชีจริงของแต่ละคนถูกล้างตอนเขา
เปิดเกมครั้งถัดไป (เข้าช้าแค่ไหนก็โดนแน่นอน 1 ครั้งต่อคำสั่ง 1 ใบ ไม่รีเซ็ตซ้ำ)

ไม่ต้องใช้กุญแจแอดมิน/รันสคริปต์อีกแล้ว — `tools/reset-leaderboard.mjs` เก็บไว้เป็นตัวสำรอง

### 3. กำหนดจำนวนวันต่อซีซัน
ตั้งได้ 1–365 วัน (ค่าเริ่มต้น 14) มีผลกับซีซันที่กำลังเดินอยู่ทันที ไม่ต้อง deploy ใหม่

### 4. ประกาศกลางจอ
พิมพ์หัวข้อ + เนื้อหา (ขึ้นบรรทัดใหม่ได้ สูงสุด 1,200 ตัวอักษร) กด "บันทึกและแสดงประกาศ"
ทุกคนจะเห็นตอนเข้าเกม กดรับทราบแล้วหาย · แก้ข้อความเมื่อไหร่ทุกคนเห็นใหม่อีกครั้ง
· มีปุ่มปิดประกาศแยก

## ยังไม่มีในรอบนี้ (ยกไปรอบที่ 2)
สร้าง card pack เอง · เพิ่ม-ลบนักเตะในร้านแลกแต้ม · แบนผู้เล่น

## เพดานความปลอดภัยที่ใส่ไว้
- ของขวัญต่อใบ: ไม่เกิน 100,000,000 ต่อสกุล และไม่เกิน 30 การ์ด (คุมทั้งในโค้ดและใน rules)
- ผู้รับไม่เชื่อตัวเลขในใบทั้งดุ้น — บีบค่าซ้ำอีกชั้นที่เครื่องตัวเองเสมอ
- การ์ดที่ชี้ไปหานักเตะที่ไม่มีอยู่จริงถูกทิ้ง ไม่ให้คลังมีใบผี
- `keep` ต้องอยู่ 0–1 · `seasonDays` ต้องอยู่ 1–365 (คุมใน rules ด้วย)
- แอดมินเขียนได้แค่ `config/*`, `gifts/*` และ `profiles/*` — **`accounts/*` ยังล็อกไว้ที่เจ้าของบัญชีคนเดียวเหมือนเดิม** เหรียญและคลังการ์ดของผู้เล่นจึงแตะไม่ได้จากภายนอก

## เทส
`npm test` 34 ข้อ (เพิ่มเรื่องคำสั่งรีเซ็ต, ประกาศ, เพดานของขวัญ, เมนู ADMIN โผล่เฉพาะเจ้าของ)

---

# APEX ELEVEN — อัปเดต: ลีกประจำวันเจอคนจริง + รางวัลการ์ดตามอันดับ

## 1. หน้า MATCH — ลีกประจำวันเป็นผู้เล่นจริงทั้งลีก

### ลีกละ 10 ทีม OVR ใกล้เคียงกัน
- คู่แข่งทั้งลีกมาจากโปรไฟล์จริงบน Firestore ไม่ใช่บอทอีกต่อไป
- ระบบเรียงผู้เล่นทุกคนตาม OVR แล้วตัด "หน้าต่าง 10 คน" ที่มีเราอยู่ตรงกลาง
  → ทุกคนในลีกจึงค่าพลังไล่เลี่ยกัน (ดู `buildLeagueMembers` ใน `services/league.ts`)
- **OVR ขยับเมื่อไหร่ ย้ายลีกทันที** — รายชื่อลีกถูกคำนวณใหม่ทุกครั้งที่ประมวลผลรอบ
  ตีบวกนักเตะหรือเปลี่ยนตัวจนค่าพลังเปลี่ยน รอบถัดไปจะถูกจัดเข้ากลุ่มใหม่ให้เอง
- ตารางแข่งใช้ round-robin แบบวงกลม: 10 ทีมเจอกันครบทุกคู่ใน 9 รอบ แล้ววนใหม่
  รอบเดียวกันไม่มีใครถูกจับคู่ซ้อนสองนัด (มีเทสคุมไว้)

### ดูทีมของคนอื่นในลีกได้
กดที่แถวไหนก็ได้ในตารางอันดับประจำวัน → เปิดดูตัวจริง 11 คนของทีมนั้น
(ใช้หน้าต่างตัวเดียวกับหน้า Leaderboard — `SquadPreviewModal`)

### เอาแมตช์กระชับมิตรออกจากหน้านี้
ระบบ Matchmaking อยู่ที่แถบแดชบอร์ดล่างของหน้า MY TEAM อยู่แล้ว ไม่ต้องมีสองทางเข้า
- แผงถ่ายทอดสดย้ายไปอยู่ที่ `MainLayout` แทน → กดหาคู่จาก MY TEAM แล้วเปลี่ยนหน้าไปดูอย่างอื่น
  ระหว่างแข่งก็ยังดูต่อได้ (แต่ไม่โผล่ทับหน้า MY TEAM เพราะที่นั่นมีแผงของตัวเองแล้ว)
- แผงรางวัลคอลัมน์ขวาตัดหัวข้อ "รางวัลกระชับมิตร" ออก เหลือเฉพาะเรื่องลีก

### ผลย้อนหลังเหลือเฉพาะลีก
ตัดแท็บ ทั้งหมด/ลีก/กระชับมิตร ออก แสดงเฉพาะนัดที่ `mode === 'league'`

### ข้อจำกัดที่ต้องรู้ (เกมนี้ไม่มีเซิร์ฟเวอร์คอยแข่งให้)
คนในตารางเป็น**ผู้เล่นจริง ค่าพลังจริง ทีมจริง** แต่ผลของนัดที่ *เขาเตะกันเอง*
ถูกจำลองจาก seed คงที่ (วัน + uid ของเขา) เปิดกี่ครั้งก็ได้ผลเดิม ไม่สุ่มใหม่
— ดู `getMemberDaily`. ถ้าวันหนึ่งมี backend จริง ให้เปลี่ยนเฉพาะฟังก์ชันนี้
ไปอ่านผลจริงจากเซิร์ฟเวอร์ ส่วนอื่นไม่ต้องแก้

## 2. หน้า LEADERBOARD — การ์ดรางวัลอันดับ 1–10

### แถวโชว์รางวัลบนสุดของหน้า
- **อันดับ 1 อยู่ตรงกลาง** แล้วไล่ออกซ้าย-ขวาสลับกันเป็นอันดับ 2–10
  (ลำดับจริง = `SHOWCASE_ORDER = [10, 8, 6, 4, 2, 1, 3, 5, 7, 9]`)
- ใบอันดับ 1 ตัวใหญ่สุดพร้อมแสงทอง · จอแคบเลื่อนแนวนอนได้ และถูกเลื่อนมาให้ใบอันดับ 1
  อยู่กลางจอตั้งแต่เปิดหน้า
- อันดับที่เรากำลังยืนอยู่จะมีขอบเขียวไฮไลต์ไว้
- **อันดับ 11 ลงไป ได้แพ็คสุ่มการ์ด 10 ใบเท่ากันทุกคน**
- รางวัลจ่ายตอนจบซีซัน — การ์ดเข้าคลังตอนกดรับในหน้าจอสรุปซีซัน (เห็นการ์ดที่ได้ก่อนกดรับ)

### เจ้าของโปรเจคเลือกการ์ดเองได้ (2 วิธี)
**วิธีที่ 1 — แก้ในไฟล์** (ต้อง deploy ใหม่)
แก้รายการ `DEFAULT_RANK_REWARDS` ใน `src/data/rankRewards.ts` (ดู id การ์ดที่ `src/data/roster.ts`)

**วิธีที่ 2 — แก้ในเกม** (มีผลทันที ไม่ต้อง deploy)
1. ใส่ไอดีผู้เล่นของคุณใน `OWNER_USERNAMES` (`src/data/rankRewards.ts`) — ค่าเริ่มต้นคือ `owner`, `admin`
2. ล็อกอินด้วยไอดีนั้น → หน้า Leaderboard → ปุ่ม **"ตั้งค่ารางวัล"** (คนอื่นไม่เห็นปุ่มนี้)
3. เลือกการ์ดให้แต่ละอันดับจากคลังนักเตะทั้งเกม (ค้นหาด้วยชื่อ/ตำแหน่ง/ระดับได้) → กดบันทึก
4. ค่าถูกเก็บที่ Firestore `config/rankRewards` ผู้เล่นทุกคนเห็นชุดใหม่ทันที

> ก่อนใช้วิธีที่ 2 ต้องเปิดสิทธิ์เขียนก่อน: เอา **uid** ที่แสดงอยู่มุมล่างซ้ายของหน้าต่าง
> "ตั้งค่ารางวัล" ไปใส่ในฟังก์ชัน `isProjectOwner()` ใน `firestore.rules` แล้ว deploy rules
> (`firebase deploy --only firestore:rules`) ถ้ายังไม่ทำ กดบันทึกจะขึ้นข้อความบอกให้ทราบ

ตั้ง id การ์ดผิด/การ์ดถูกลบไปแล้ว = ระบบถอยไปใช้ค่าเริ่มต้นของอันดับนั้นให้เอง เกมไม่พัง

## เทส
`npm test` 21 ข้อ · `npm run smoke` ครอบคลุมการจัดลีก ตารางลีก และรางวัลตามอันดับเพิ่มแล้ว

---

# APEX ELEVEN — อัปเดต: ตารางอันดับแสดงทุกคน + แบ่งหน้า

## ที่เปลี่ยน
- เดิมดึงมาแค่ 100 อันดับแรก ตอนนี้ดึงผู้เล่น **ทุกคนที่สมัครเข้ามา**
- ตารางแบ่งหน้าละ **20 แถว** พร้อมแถบเลขหน้า (ย่อด้วยจุดไข่ปลาเมื่อเกิน 7 หน้า)
- ใต้ตารางบอก "หน้า 2/5 · แสดงอันดับ 21–40 จาก 93 ทีม"
- ปุ่มลัด **"ไปอันดับของฉัน (#37)"** โผล่เมื่อเราไม่ได้อยู่ในหน้าที่เปิดค้างไว้

## จุดที่ต้องระวังเวลาแก้ต่อ
- **อันดับคำนวณจากรายชื่อทั้งหมดก่อนแบ่งหน้า** เลข # ในตารางจึงเป็นอันดับจริงของทั้งเซิร์ฟเวอร์
  ไม่ใช่ลำดับที่ 1–20 ของหน้านั้น (มีเทสคุมไว้แล้ว)
- ถ้าจำนวนผู้เล่นลดลงระหว่างเปิดหน้าค้างไว้ (เช่นซีซันรีเซ็ต) หน้าที่เปิดอยู่อาจหายไป
  ระบบจะดึงกลับมาหน้าสุดท้ายให้เอง ไม่ปล่อยให้เห็นตารางว่าง

## เพดานที่ยังเหลืออยู่ (`LEADERBOARD_LIMIT = 500`)
ยังต้องมีเพดาน เพราะโปรไฟล์ทุกคน (รวมรูป) ถูกโหลดมาทั้งก้อนทุกครั้งที่เปิดเกม
500 คนพอสำหรับเซิร์ฟเวอร์เล่นกับเพื่อน ถ้าวันหนึ่งเกินจริง ต้องเปลี่ยนไปดึงทีละหน้า
จากเซิร์ฟเวอร์ (query แบบ `startAfter`) แทนการโหลดทั้งหมดมาแบ่งหน้าที่หน้าเว็บ
— จุดที่ต้องแก้คือ `watchTopProfiles` ใน `services/firebase/profiles.ts`

---

# APEX ELEVEN — อัปเดต: หน้า SETTINGS

## ที่ตั้งรูปโปรไฟล์ย้ายมาที่นี่
เมนู **SETTINGS** (เดิมปิดอยู่) เปิดใช้งานแล้ว และเป็นที่รวมของที่ "ตั้งครั้งเดียวแล้วจบ":
- **รูปโปรไฟล์** — อัปโหลด / เปลี่ยน / ลบ (ย้ายออกจากหน้า Profile)
- **เสียงเอฟเฟกต์** — เปิด/ปิด (ค่าเดียวกับปุ่มลำโพงบนแถบหัว)
- **บัญชี** — ไอดี, ชื่อสโมสร, สถานะว่าเซฟอยู่บนเซิร์ฟเวอร์หรือในเครื่อง, ปุ่มออกจากระบบ

หน้า **PROFILE** เหลือเฉพาะของที่ดูบ่อย — คลังการ์ด สถิติซีซัน ระดับ และระบบย่อยการ์ด

## ทางลัด
กดที่รูป/ชื่อมุมขวาบนของแถบหัว = ไปหน้า Settings ทันที
(คนส่วนใหญ่กดตรงนั้นเป็นสัญชาตญาณเวลาจะเปลี่ยนรูป)

## เทส
เพิ่มหน้า Settings เข้าไปในเทส "เข้าทุกหน้าจากเมนูได้โดยไม่พัง" แล้ว

---

# APEX ELEVEN — อัปเดต: รูปโปรไฟล์

## ⚠️ ต้อง Publish `firestore.rules` ใหม่
กฎเพิ่มเพดานขนาดของฟิลด์ `avatar` ถ้าไม่อัปเดต รูปจะเซฟลงบัญชีได้แต่คนอื่นจะไม่เห็น
(การประกาศโปรไฟล์สาธารณะจะถูกปฏิเสธทั้งก้อน)

## ใช้งาน
- หน้า **PROFILE** ด้านบนสุด → ปุ่ม "อัปโหลดรูป" (เปลี่ยน/ลบได้ตลอด)
- รูปจะไปโผล่ที่: แถบหัวมุมขวาบน · ทุกแถวในตารางอันดับ · หัวหน้าต่าง "ดูทีม" ของคู่แข่ง
- เปลี่ยนรูปแล้วเห็นทุกเครื่องที่ล็อกอินไอดีเดียวกัน ส่วนคนอื่นเห็นภายในไม่กี่วินาที

## ทำไมไม่ใช้ Firebase Storage
Storage ต้องอัปเป็นแพลนแบบผูกบัตร (Blaze) ส่วน Firestore ใช้ได้ฟรีตั้งแต่แรก
ระบบจึงย่อรูปในเครื่องผู้เล่นก่อน แล้วเก็บเป็นข้อความ data URL ในเอกสารบัญชีเลย
ไม่ต้องตั้งค่าอะไรเพิ่ม และไม่มีไฟล์ค้างให้ต้องตามลบเวลาผู้เล่นเปลี่ยนรูป

## การย่อรูป (`services/avatar.ts`)
- ครอบตัดจากกึ่งกลางเป็นสี่เหลี่ยมจัตุรัส แล้วย่อเหลือ **112×112 px** (รูปแนวตั้ง/นอนจึงไม่เบี้ยว)
- บันทึกเป็น webp คุณภาพ 0.72 · Safari รุ่นเก่าที่เขียน webp ไม่ได้จะถอยไปใช้ jpeg อัตโนมัติ
- ผลลัพธ์ปกติ 4–8 KB ต่อรูป · รับไฟล์ต้นทางไม่เกิน 8 MB
- **ขนาดเล็กเป็นเรื่องตั้งใจ**: รูปนี้เดินทางไปพร้อมโปรไฟล์สาธารณะที่ทุกคนโหลด
  พร้อมตารางอันดับ 100 แถว รูปใหญ่ขึ้น 10 เท่า = เน็ตมือถือของทุกคนหนักขึ้น 10 เท่า

## ความปลอดภัย
- รูปจากผู้เล่นคนอื่นถือเป็นข้อมูลภายนอก ต้องผ่าน `isSafeAvatar` ก่อนเอาไปเป็น src ของ `<img>`
- อนุญาตเฉพาะ data URL ของ webp/jpeg/png เท่านั้น — `javascript:`, ลิงก์เว็บนอก
  และ **svg** ถูกปฏิเสธทั้งหมด (svg ฝัง script ได้ และลิงก์นอกใช้ดักไอพีคนดูได้)
- กฎ Firestore คุมเพดานไว้ที่ 60,000 ตัวอักษร ตรงกับ `AVATAR_MAX_CHARS` ในโค้ด

## เทสที่เพิ่ม (`npm run smoke`)
ครอบคลุม: รูปที่ถูกต้องใช้ได้ · javascript:/ลิงก์นอก/svg/รูปเกินเพดานถูกปฏิเสธ ·
ไม่มีรูปแล้วขึ้นตัวอักษรแรกแทน · รูปอันตรายไม่หลุดออกไปอยู่ใน HTML

---

# APEX ELEVEN — อัปเดต: ส่องตัวจริง 11 คนของคู่แข่งจากตารางอันดับ

## ⚠️ ต้อง Publish `firestore.rules` ใหม่อีกครั้ง
กฎเพิ่มเงื่อนไขของฟิลด์ `squad` ในโปรไฟล์สาธารณะ (ไม่เกิน 11 ช่อง)
ถ้าไม่อัปเดต การประกาศตัวจริงจะถูกปฏิเสธ แล้วทุกทีมจะขึ้นว่า "ยังไม่ได้ประกาศตัวจริง"

## ใช้งาน
- หน้า **LEADERBOARD** แถวของผู้เล่นจริงจะมีป้าย "ดูทีม" — กดแถวไหนก็ได้เพื่อเปิดดู
- ในหน้าต่างที่เด้งขึ้นมาจะเห็น: สนามพร้อมการ์ด 11 ใบวางตามแผนจริงของเขา,
  ชื่อแผนการเล่น, Team OVR, และรายชื่อแบบตารางพร้อมค่าตีบวก (+1, +2, …) กับ OVR จริงในช่องนั้น
- ทีมจำลอง (แถวที่ระบบเติมให้ตอนผู้เล่นยังน้อย) กดไม่ได้ เพราะไม่มีตัวจริงจริง ๆ
- แถวของตัวเองก็กดดูได้ — ใช้เช็กว่าทีมที่คนอื่นเห็นตรงกับที่จัดไว้ล่าสุดไหม

## กลไก
- `profiles/{uid}` เพิ่มฟิลด์ `squad` เก็บแค่ `slotId + playerId + level` ของ 11 ช่อง
  รายละเอียดนักเตะ (ชื่อ/ค่าพลัง/รูป) อ่านจาก `data/players.ts` ของเครื่องคนดูเอง
  จึงส่งข้อมูลข้ามเครื่องแค่ไม่กี่ร้อยไบต์ต่อทีม
- ประกาศพร้อมโปรไฟล์เดิมใน `useOnline.tsx` (หน่วง 4 วินาทีหลังหยุดจัดทีม)
  ทีมเปลี่ยนเมื่อไหร่ คนอื่นเห็นของใหม่ทันทีในรอบถัดไป
- `components/leaderboard/SquadPreviewModal.tsx` — สนามอ่านอย่างเดียว
  ใช้ perspective ชุดเดียวกับหน้า MY TEAM (`projectToPitch`) แต่ตัดลากวาง/สลับตัวออกหมด
- ช่องที่เจ้าของทีมยังไม่ได้จัดจะขึ้นเป็นกรอบประ ไม่ใช่การ์ด

## หมายเหตุ
- ข้อมูลเป็นภาพนิ่งของตอนที่เจ้าของทีมเข้าเกมครั้งล่าสุด — เขาอาจสลับตัวไปแล้วก็ได้
- บัญชีที่ไม่ได้เปิดเกมเลยหลังอัปเดตนี้จะยังไม่มีตัวจริงให้ดู จนกว่าจะเข้าเกมอีกครั้ง

---

# APEX ELEVEN — อัปเดต: ผลแมตช์ตกถึงคู่แข่งด้วย (กล่องผลการแข่ง)

> เดิมเวลาเราท้าทีมของเพื่อน คะแนนขยับเฉพาะฝั่งเรา ฝั่งเพื่อนไม่รู้เรื่องเลย
> รอบนี้ผลตกถึงทั้งสองฝั่งแล้ว โดยไม่ต้องให้ทั้งคู่ออนไลน์พร้อมกัน

## ⚠️ ต้อง Publish `firestore.rules` ใหม่ก่อนใช้งาน
กฎชุดใหม่มีสิทธิ์ของ `matchInbox` เพิ่มเข้ามา ถ้าไม่อัปเดต การส่งผลจะโดนปฏิเสธเงียบ ๆ
(Firebase Console → Firestore Database → Rules → วางทับ → Publish)

## กลไก
- `src/services/firebase/matchInbox.ts` — ผู้ท้าเขียน "ใบรายงานผล" ลง
  `matchInbox/{uid ของคู่แข่ง}/items/{id}` ฝั่งคู่แข่งเฝ้ากล่องของตัวเองด้วย onSnapshot
  พอเปิดเกมเมื่อไหร่ก็เจอใบที่ค้างอยู่ อัปเดตสถิติ แล้วลบใบทิ้ง
- ไม่ต้องใช้ Cloud Functions (ซึ่งต้องขึ้นแพลนเสียเงิน) และไม่ต้องรอให้ใครออนไลน์
- สกอร์ในใบเขียนจากมุมของผู้รับมาแล้ว รวมถึงไทม์ไลน์ประตูที่กลับด้านให้เรียบร้อย

## สิ่งที่ฝ่ายตั้งรับได้รับ
- คะแนน ranking ตามผล (ชนะ/เสมอ/แพ้) คิดด้วยสูตรเดียวกับการลงแข่งเอง
- เหรียญแบบลดหลั่น: ชนะ 500 · เสมอ 200 · แพ้ 0 — น้อยกว่าลงแข่งเอง
  เพราะไม่ได้ลงมือเล่น (กันเปิดเกมทิ้งไว้แล้วรวยจากการถูกรุมท้า)
- นัดที่โดนท้า **ไม่นับ** เข้าภารกิจรายวันและไม่ให้แต้มตีบวก — ตั้งใจให้เป็นแบบนั้น
  เพื่อไม่ให้ฟาร์มภารกิจด้วยการนัดกันท้าไปมา
- ประวัติการแข่งขึ้นป้าย "ถูกท้า" แยกจากลีก/กระชับมิตร
- แจ้งเตือนมุมขวาบนตอนเปิดเกม (`components/matchmaking/DefenseNotice.tsx`) ปิดเองใน 9 วินาที

## ความปลอดภัย
- ผู้ส่งกำหนดได้แค่ **สกอร์** เท่านั้น ตัวเลขเหรียญและคะแนนคำนวณที่ฝั่งผู้รับเสมอ
- กฎ Firestore บังคับ: เซ็นชื่อตัวเอง (`fromUid == auth.uid`), ส่งให้ตัวเองไม่ได้,
  แก้ใบที่ส่งไปแล้วไม่ได้, สกอร์ 0–20 และค่าพลัง 0–120 เท่านั้น
- ฝั่งรับยังบีบค่าซ้ำอีกชั้นก่อนใช้ (`buildDefenseResult`)
- กันนับซ้ำสองด่าน: ไอดีที่บันทึกแล้วในเซสชันนี้ + ไอดีที่อยู่ในประวัติการแข่งอยู่แล้ว

---

# APEX ELEVEN — อัปเดตรอบนี้: ระบบออนไลน์ (Firebase) + ขึ้นเว็บจริงบน Vercel

> รอบนี้เปลี่ยนเกมจาก "เซฟในเครื่องใครเครื่องมัน" เป็น "เซิร์ฟเวอร์เดียวที่ทุกคนเจอกัน"
> คู่มือติดตั้งทีละขั้นอยู่ที่ `DEPLOY-ONLINE.md`

## 1. บัญชีและเซฟย้ายขึ้นคลาวด์
- `src/services/firebase/config.ts` — เปิดใช้ Firebase จากค่า `VITE_FIREBASE_*`
  ถ้าไม่ได้ตั้งค่า แอปจะ **ไม่พัง** แต่ตกลงไปเล่นโหมดออฟไลน์แบบเดิม
- `src/services/firebase/cloudAccount.ts` — สมัคร/ล็อกอิน/โหลดเซฟจาก Firebase Auth + Firestore
  ไอดีผู้เล่น (ภาษาไทยก็ได้) ถูกแปลงเป็นอีเมลภายในด้วย SHA-256 จึงไม่ต้องมีตารางค้นหา
  และ Firebase กันไอดีซ้ำให้เอง
- `src/services/accountStore.ts` — ยังเป็นประตูเดียวเหมือนเดิม แต่ตอนนี้เลือกเองว่าจะคุยกับคลาวด์
  หรือ localStorage ส่วนอื่นของแอปไม่ต้องรู้
- เซฟขึ้นคลาวด์แบบ **หน่วง 2.5 วินาที** (กันเขียนรัวตอนลากตัวผู้เล่น) และบังคับเซฟทันที
  ตอนปิดแท็บ/สลับแอป/ออกจากระบบ — ลงเครื่องยังเซฟทันทีทุกครั้งเหมือนเดิม
- ล็อกอินค้างไว้ได้ข้ามเครื่อง: เปิดแอปแล้วมีจอ "กำลังเชื่อมต่อเซิร์ฟเวอร์…" สั้น ๆ ก่อนเข้าเกม

## 2. ตารางอันดับเป็นผู้เล่นจริง อัปเดตสด
- `src/services/firebase/profiles.ts` — ทุกบัญชีประกาศข้อมูลย่อของตัวเอง (ชื่อทีม, ค่าพลัง,
  คะแนน, สถิติ) ลง collection `profiles` แล้วติดตามอันดับ 100 อันดับแรกแบบเรียลไทม์
- `src/hooks/useOnline.tsx` — ตัวกลางที่ประกาศตัวเอง + แปลงข้อมูลคนอื่นให้พร้อมใช้
- คนอื่นชนะแล้วคะแนนขยับ ตารางของเราขยับตามทันทีโดยไม่ต้องรีเฟรช
- ผู้เล่นจริงยังไม่ถึง 12 คน จะเติมทีมจำลองให้ตารางไม่โล่ง แล้วถูกเบียดออกเองเมื่อคนเยอะขึ้น
- หน้าตารางอันดับมีไฟสถานะบอกว่ากำลังดูอันดับสดอยู่ หรือยังเป็นตารางออฟไลน์

## 3. จับคู่กับทีมของผู้เล่นจริง
- `findOpponent` ใน `src/services/matchmaking.ts` รับ "สระคู่แข่ง" เข้ามาแล้ว
  ลำดับการหา: ผู้เล่นจริงที่พลังใกล้เคียง (85%) → ทีมประจำระบบ → บอทสุ่ม
- เจอชื่อทีม ค่าพลัง และแผนการเล่นของคนจริง ๆ ไม่ใช่ชื่อสุ่มอีกต่อไป
- ยังเป็นการจำลองผลฝั่งเรา (ไม่ต้องรอคู่แข่งออนไลน์พร้อมกัน) — ถ้าจะให้ชนกันสองฝ่ายจริง
  ต้องต่อ Cloud Functions เพิ่มในอนาคต

## 4. พร้อมขึ้น Vercel
- `vercel.json` — rewrite ทุก path ไป `index.html` (ไม่งั้นเข้า `/leaderboard` ตรง ๆ จะ 404)
  พร้อมตั้งแคชไฟล์ static แบบถาวร
- `vite.config.ts` — แยกก้อน `react` / `firebase` ออกจากโค้ดเกม เปิดเว็บครั้งถัดไปไวขึ้น
- `.env.example` — รายการค่าที่ต้องกรอกทั้งใน `.env` และหน้า Environment Variables ของ Vercel
- `firestore.rules` + `firebase.json` — กฎความปลอดภัย: เซฟของใครของมัน,
  โปรไฟล์สาธารณะอ่านได้ทุกคนแต่เขียนได้เฉพาะเจ้าของ และตรวจชนิด/ช่วงค่าก่อนรับ
  กันคนยิง API แก้คะแนนตัวเองแบบมั่ว ๆ

## 5. แก้บั๊กที่ทำให้ `npm run build` ไม่ผ่าน (ค้างมาจากรอบก่อน)
- `src/data/cards.ts` — ซอง Signature Pack กรอกอัตราส่วนไม่ครบทุกระดับ (ขาด rare/epic)
- `src/hooks/useTeam.tsx` — หน้าเปลี่ยนตัวยังเรียก `squadLock` ที่ถูกถอดออกไปแล้ว
  จึงคืนค่ากลับมาเป็น "ปลดล็อกเสมอ" ไว้ที่เดียว (ถ้าจะเปิดคูลดาวน์อีกครั้งแก้ตรงนั้นจุดเดียว)
- เพิ่ม `src/vite-env.d.ts` ให้ TypeScript รู้จัก `import.meta.env`
- ผลคือ `npm run build` ผ่านสะอาด — Vercel จึง deploy ได้ (ก่อนหน้านี้จะ build ล้มทันที)

---

# FC ALLSTAR — สรุปสิ่งที่เพิ่ม/แก้ในรอบนี้

## 1. ห้ามนักเตะชื่อเดียวกันลงทีม 11 ตัวจริง
- กติกาอยู่ที่ `src/hooks/useTeam.tsx` — เป็นทางผ่านของทุกวิธีจัดตัว (ลากวาง, จิ้มเลือก,
  เปลี่ยนแผน, จัดอัตโนมัติ) จึงเลี่ยงไม่ได้ไม่ว่าจะเข้าทางไหน
- เทียบชื่อแบบไม่สนตัวพิมพ์ใหญ่-เล็กและช่องว่างหน้า-หลัง (`HAALAND` = `Haaland`)
- มีการ์ดชื่อซ้ำในคลังได้ แต่เลือกลงสนามได้ใบเดียว
- สลับตำแหน่งของคนที่อยู่ในสนามอยู่แล้วยังทำได้ปกติ (ไม่ได้เพิ่มชื่อใหม่)
- UI: ในหน้าต่างเลือกนักเตะ คนที่ชื่อซ้ำเป็นการ์ดขาวดำ กดไม่ได้ + ป้าย "ชื่อซ้ำในทีม"
  และถูกดันไปท้ายรายการ / ถ้าลากไปวางจะขึ้นข้อความเตือนกลางสนาม 3 วินาที

## 2. เสียงเอฟเฟกต์ตอนเปิดการ์ด
- `src/services/sound.ts` สังเคราะห์เสียงด้วย Web Audio API ล้วน ไม่ต้องแนบไฟล์ mp3
  (ขนาดบิลด์เท่าเดิม และไม่มีดีเลย์รอโหลดไฟล์ตอนซองแตก)
- ผูกกับ 4 จังหวะของฉากเปิดซอง:
  1. ซองลอยขึ้น — เสียงคลื่นต่ำ
  2. ซองสั่นสะสมพลัง — เสียงไต่ระดับ ยิ่งการ์ดดียิ่งไต่สูงและนานตาม `tearMs` จริง
  3. ซองแตก — เสียงระเบิด + คลื่นกระแทก
  4. การ์ดโผล่ — คอร์ดแยกตามระดับ (legendary ได้แฟนแฟร์เต็ม + เสียงอัฒจันทร์)
- เสียงอื่น: ซื้อซอง, ย่อยการ์ด, สลับตัว, ชนะ/แพ้, เลื่อนขั้น rank, เข้าสู่ระบบ, เตือนเมื่อทำไม่ได้
- ปุ่มเปิด/ปิดเสียงอยู่บน Header จำค่าไว้ใน localStorage

## 3. สมัครไอดี → เงิน 1 ล้าน + ผู้เล่นเริ่มต้น
- หน้าสมัคร/เข้าสู่ระบบ: `src/pages/Auth/AuthPage.tsx`
- ระบบบัญชี: `src/hooks/useAuth.tsx` + `src/services/accountStore.ts`
- บัญชีใหม่ได้ **1,000,000 เหรียญ** + **นักเตะ 17 คน** ครบทุกตำแหน่งของ 4-3-3
  (แก้ค่าเริ่มต้นได้ที่ `src/data/starter.ts` ที่เดียว)
- เหรียญ / แต้ม / คลังการ์ด / การจัดทีม / สถิติซีซัน เซฟอัตโนมัติ ไม่หายตอนรีเฟรช
- ล็อกอินค้างไว้ได้ และสลับบัญชีได้ (แต่ละไอดีมีคลังของตัวเอง)
- ⚠️ รหัสผ่านถูกแปลงฝั่ง client เพื่อไม่เก็บรหัสดิบเท่านั้น **ไม่ใช่ระบบความปลอดภัยจริง**
  ตอนต่อ backend ให้แก้เฉพาะ `accountStore.ts` ส่วนอื่นของแอปไม่ต้องแตะ

## 4. ย่อยการ์ดเป็นแต้ม (เพดาน 5,000)
- สูตรอยู่ที่ `src/services/salvage.ts`: ค่าฐานตามระดับการ์ด + โบนัสตาม OVR + โบนัสตามเลเวล
  แล้วบีบไม่ให้เกิน **5,000 แต้มต่อใบ** เสมอ
- ค่าปัจจุบัน: common ~330–430 · rare ~1,500 · epic ~3,000 · legendary เต็มเพดาน 5,000
  (การ์ดที่เต็มเพดานจะมีดาว ★ กำกับ)
- UI อยู่ในหน้า My Cards: แตะเลือกหลายใบพร้อมกัน เห็นยอดรวมก่อน แล้วมีหน้ายืนยันก่อนย่อยจริง
- การ์ดที่อยู่ใน 11 ตัวจริงย่อยไม่ได้ ต้องเอาออกจากสนามก่อน

## 5. ระบบ rank 5 ระดับ
- `src/services/rank.ts` — BRONZE → GOLD → PLATINUM → LEGEND → CHAMPION
- เกณฑ์: 0 / 300 / 800 / 1,500 / 2,500 คะแนน (แก้ `minPoints` ที่ตารางเดียวจบ)
- ป้ายระดับขึ้นบน Header, ตารางอันดับ และหน้าโปรไฟล์ (มีหลอดความคืบหน้าไประดับถัดไป)
- เลื่อนขั้นแล้วมีเสียงฉลอง
- ปรับคะแนนของทีมในตารางอันดับจำลองให้สเกลสอดคล้องกับเกณฑ์ใหม่แล้ว

## 6. ฉายา 1ST CHAMPION สีทอง
- เป็นของ **อันดับ 1 คนเดียวเท่านั้น** ไม่ขึ้นกับคะแนน (คนละเรื่องกับระดับ CHAMPION)
- ป้ายไล่เฉดทองพร้อมแสงเรือง แสดงที่: ตารางอันดับ (แถวที่ 1 มีแถบทองด้วย),
  วิดเจ็ตอันดับบนแดชบอร์ด และบน Header ของผู้เล่นเองเมื่อขึ้นเป็นอันดับ 1

## 7. ร้านแลกนักเตะด้วยแต้ม (เพิ่มรอบนี้)
- เมนู **Exchange** อยู่ใต้ Card Pack ในแถบเมนู (ทั้ง Sidebar และเมนูล่างบนมือถือ)
- หน้า `src/pages/Exchange/ExchangePage.tsx` โชว์การ์ดนักเตะทุกคนที่แลกได้
  พร้อมรูปการ์ด ตำแหน่ง OVR ระดับการ์ด และราคาเป็นแต้ม
- ตัวกรอง: ตามระดับการ์ด (Legendary/Epic/Rare/Common), "แลกได้ตอนนี้" และช่องค้นหาชื่อ
- การ์ดที่มีอยู่แล้วในคลังจะมีป้าย "มีแล้ว x" มุมบนขวา
  (เตือนไว้เพราะนักเตะชื่อซ้ำลงตัวจริงพร้อมกันไม่ได้)
- กดแลก → หน้ายืนยันแสดงการ์ดใหญ่ ราคา และแต้มคงเหลือหลังแลก
- แลกสำเร็จ → ใช้ฉากเผยการ์ดชุดเดียวกับการเปิดซอง จึงได้เอฟเฟกต์และเสียงครบ
- การ์ดที่ได้เข้าคลังเป็น **ตัวสำรอง** ไม่ลงตัวจริงอัตโนมัติ (กันชนกฎห้ามชื่อซ้ำ)

### สูตรราคา (`src/services/exchange.ts`)
    ราคาแลก = แต้มที่ได้จากการย่อยการ์ดใบนั้น × 3   (ปัดทีละ 50)

ราคาปัจจุบัน: common 1,000–1,300 · rare ~4,450 · epic ~9,000 · legendary 15,000

ตั้งเป็น "คูณ" แบบนี้เพราะทำให้ย่อยแล้วซื้อคืนขาดทุนเสมอ ผู้เล่นจึงหมุนแต้มวนไม่รู้จบไม่ได้
**อย่าปรับ `EXCHANGE_RATE` ลงต่ำกว่า 1** เพราะจะเปิดช่องโหว่ปั๊มแต้มทันที
ถ้าอยากทำเป็นร้านหมุนเวียนรายวัน แก้ที่ `getExchangeCatalogue()` ที่เดียว

### บั๊กข้อมูลที่เจอตอนทำหน้านี้
- `src/data/roster.ts` มี `p029.gif` ซ้ำสองบรรทัด (KVARATSKHELIA กับ MARQUINHOS)
  ทำให้นักเตะสองคนมี id เดียวกัน → `getPlayerById` คืนคนแรกเสมอ อีกคนหายจากเกมเงียบ ๆ
  และ React เตือนเรื่อง key ซ้ำ แก้เป็น `p028.gif` (ไฟล์มีอยู่แล้วแต่ไม่เคยถูกใส่ในรายชื่อ)
  **ถ้ารูปสองใบนี้สลับคนกัน ให้สลับชื่อสองบรรทัดนั้นได้เลย**
- เพิ่มตัวเตือนใน `src/data/players.ts` — ถ้ามี id ซ้ำอีกจะ `console.warn` บอกทันที

## 8. พัฒนานักเตะ (Level Up) + รวมร่างการ์ดซ้ำ
- `src/services/upgrade.ts` — เลเวลสูงสุด **LV5**, ทุกเลเวล **+2 OVR** (สูงสุด +8)
- อัปได้ 2 ทาง:
  - **รวมร่างการ์ดซ้ำ (ฟรี)** — ใช้การ์ดของนักเตะคนเดียวกันอีกใบ ได้ +1 LV ไม่เสียแต้ม
    ทางนี้แก้ปัญหาที่กฎห้ามชื่อซ้ำสร้างไว้โดยตรง: เดิมเปิดซองได้คนเดิมซ้ำ = ของไร้ค่า
  - **จ่ายแต้ม** — ราคา = ค่าฐานตามระดับการ์ด × เลเวลปัจจุบัน
    (common 500 · rare 1,200 · epic 2,400 · legendary 4,000 ต่อขั้น)
- โบนัสถูกบวกที่ `cardPlayer()` ใน `useTeam` **จุดเดียว** — Team OVR เคมี โอกาสชนะ
  และหน้าต่างเลือกตัวจึงเห็นค่าที่อัปแล้วทันทีโดยไม่ต้องแก้ทีละที่
  ⚠️ คลังการ์ด (`ownedCards`) เก็บค่าดิบไว้เสมอ อย่าเรียก `applyLevel` ซ้อนสองครั้ง
- UI: หน้า My Cards มีสวิตช์ **"โหมดย่อยการ์ด"**
  - โหมดปกติ (ค่าเริ่มต้น) — แตะการ์ด = เปิดหน้าต่างรายละเอียด ดูค่าพลัง 6 ด้าน อัปเกรด รวมร่าง
  - โหมดย่อย — แตะการ์ด = ติ๊กเลือกย่อยเป็นแต้มแบบเดิม
- การ์ดที่อัปแล้วมีป้าย **LV** มุมบนซ้าย (ป้ายขึ้นเฉพาะ LV2+ เพื่อไม่ให้รกการ์ดปกติ)

## 9. แมตช์ถ่ายทอดสด
- `simulateMatch` ตัดสินสกอร์ **และไทม์ไลน์ประตู** ตั้งแต่วินาทีแรก
  จากนั้นนาฬิกาในเกมเดินจากนาที 0→90 แล้วค่อยเปิดเผยประตูทีละลูกตามนาทีของมัน
  จึงไม่มีทางที่ไทม์ไลน์กับผลสุดท้ายจะไม่ตรงกัน
- 90 นาทีในเกม = ราว 12 วินาทีจริง (ปรับที่ `TICK_MS` ใน `useMatchmaking.tsx`)
- ผู้ยิงสุ่มจากตัวจริงจริง ๆ ถ่วงน้ำหนักตามตำแหน่ง (กองหน้า ×4 · กองกลาง ×2 · กองหลัง ×1 · GK ไม่ยิง)
- `LiveMatchPanel` แสดงนาฬิกา สกอร์สด หลอดเวลา และไทม์ไลน์ประตู
- มีเสียงใหม่: นกหวีดเริ่ม/จบเกม, เสียงฝูงชนเฮตอนเราได้ประตู, เสียงฮือตอนเสียประตู
- กด **"ข้ามไปดูผล"** ได้ตลอด — จบทันทีด้วยผลเดิมที่สุ่มไว้แล้ว ไม่ใช่สุ่มใหม่
- หน้าจอผลการแข่งโชว์ว่าใครยิงนาทีไหนบ้าง

## 10. ซีซันและรางวัลปลายซีซัน
- `src/services/season.ts` — ซีซันละ **14 วัน** (แก้ที่ `SEASON_DAYS`)
- หมดเวลาแล้วหน้าสรุปเด้งขึ้นเอง **ต้องกดรับรางวัลก่อนถึงเล่นต่อได้**
  (ตรวจตอนเปิดแอปและตรวจซ้ำทุกนาที เปิดค้างข้ามคืนก็เห็น)
- รางวัลตามระดับที่จบ:

  | ระดับ | เหรียญ | แต้ม |
  |---|---|---|
  | BRONZE | 100,000 | 500 |
  | GOLD | 400,000 | 2,000 |
  | PLATINUM | 900,000 | 5,000 |
  | LEGEND | 1,800,000 | 12,000 |
  | CHAMPION | 3,000,000 | 25,000 |

  จบ **อันดับ 1** ได้โบนัสเพิ่ม +1,000,000 เหรียญ / +10,000 แต้ม
- ขึ้นซีซันใหม่แบบ **soft reset**: คะแนนยกไป 30% (`CARRY_OVER`) สถิติแพ้-ชนะเริ่มนับใหม่
  คนเก่งจึงไม่ต้องเริ่มจากศูนย์ แต่ผู้เล่นใหม่ก็ยังไล่ทัน
- ซีซันใหม่เริ่มนับเวลา **ตอนกดรับ** ไม่ใช่ตอนหมดอายุ — หายไปหนึ่งเดือนแล้วกลับมา
  จะไม่โดนข้ามซีซันรวดเดียวหลายรอบ
- หน้า Leaderboard โชว์ "ซีซัน N · เหลืออีก X วัน"

## 11. ตำแหน่งและเคมีมีผลจริงแล้ว
เดิม `calculateTeamRating()` คิด Team OVR จาก `player.ovr` ดิบ ไม่ได้เรียก `getEffectiveOvr()`
ที่อยู่ในไฟล์เดียวกัน ทำให้ค่าปรับผิดตำแหน่งและค่า `chemistry` เป็นแค่ตัวเลขโชว์
ยัดใครลงช่องไหนก็ได้โดยไม่เสียอะไร — ระบบจัดทีมทั้งหมดจึงไม่มีความหมายเชิงกลยุทธ์

**ที่แก้:**
- Team OVR (รวมทั้งเกมรุก/กลาง/รับ) คิดจาก `getEffectiveOvr()` แล้ว
  → ผิดตำแหน่งหลัก −4, เล่นตำแหน่งรอง −1 มีผลกับค่าพลังทีมจริง
- เพิ่ม `getChemistryBonus()` แปลงเปอร์เซ็นต์เคมีเป็นโบนัสค่าพลัง **−5 ถึง +3**
  (เกณฑ์กลางอยู่ที่ 75% ปรับได้ที่ `CHEMISTRY_NEUTRAL` / `CHEMISTRY_SCALE`)
- `TeamRating` มีสองตัวเลขแยกกันชัดเจน:
  - `ovr` — ค่าพลังพื้นฐาน (หักค่าปรับผิดตำแหน่งแล้ว) แสดงบนตราในแผง Team OVR
  - `matchOvr` — `ovr + chemistryBonus` คือตัวเลขที่ **ตัดสินแพ้ชนะจริง**
    ใช้ในการหาคู่แข่ง คำนวณโอกาสชนะ จำลองผลการแข่ง และตารางอันดับ
  แยกแบบนี้เพื่อให้อธิบายผู้เล่นได้ว่า "นักเตะเก่งเท่าเดิม แต่ทีมเล่นเข้าขากันหรือไม่"

**ผลที่วัดได้** (ทีมนักเตะชุดเดียวกันเป๊ะ ต่างกันแค่วิธีจัด):

| วิธีจัดทีม | OVR | เคมี | โบนัส | พลังลงแข่ง | โอกาสชนะทีม OVR 85 |
|---|---|---|---|---|---|
| จัดตรงตำแหน่ง | 84 | 33/33 | +3 | **87** | 50% |
| ยัดคน OVR สูงสุดลงมั่ว | 82 | 5/33 | −5 | **77** | 8% |

**UI ที่เพิ่ม:**
- แผง Team OVR มีบรรทัด "พลังตอนลงแข่ง" พร้อมบอกว่าเคมีบวกหรือลบเท่าไร
- แผง Chemistry บอกผลเป็นตัวเลข (`พลังทีม +3`) และมีคำใบ้ว่าทำยังไงให้เคมีขึ้น

**หมายเหตุ:** ค่าปรับผิดตำแหน่งกับเคมีเป็นสัญญาณเดียวกันสองชั้น (โดนหักซ้อนกัน)
ตั้งใจให้เป็นแบบนี้เพื่อให้การจัดทีมมีน้ำหนักพอจะเป็นกลยุทธ์
ถ้ารู้สึกว่าลงโทษหนักไป ลด `CHEMISTRY_SCALE` หรือ `OUT_OF_POSITION_PENALTY` ได้

## 12. Matchmaking แบบลีกประจำวัน
Matchmaking เดิม (กดหาคู่เอง) ยังอยู่ในชื่อ **แมตช์กระชับมิตร**
ส่วนระบบหลักเปลี่ยนเป็น **ลีกประจำวัน** ที่ระบบจับคู่ให้เองตามตาราง

### รอบการแข่ง
- เข้าร่วมครั้งเดียว แล้วแข่งอัตโนมัติทุก **30 นาที** (นาที :00 และ :30)
- วันแข่งเริ่ม **06:00** ไปจนถึงรอบ **05:30** ของวันถัดไป = **48 รอบ/วัน**
- ครบวันแล้วสรุปอันดับ แจกรางวัล แล้วเริ่มวันใหม่อัตโนมัติ
- **ไม่ต้องเปิดเกมค้างไว้** — เกมไม่มีเซิร์ฟเวอร์ รอบที่ผ่านไปตอนปิดแอปจึงถูก
  ย้อนคำนวณตอนเปิดกลับมา (`getRoundsBetween`) ตัดไว้สูงสุด 48 รอบต่อครั้ง
  กันเปิดครั้งเดียวแล้วรันเป็นพันรอบ
- คู่แข่งวนไปตามลำดับรอบ ทุกทีมในลีกจึงได้เจอครบทั้งวัน

### ตารางอันดับประจำวัน
- คะแนนลีกใช้ระบบ **3-1-0** เรียงอันดับด้วย คะแนน → ผลต่างประตู → ประตูได้
- บอทในลีกมีผลของตัวเองที่ **ขยับจริงทุกรอบ** ไม่ใช่ตัวเลขค้างนิ่งแบบตารางซีซัน
  ใช้ PRNG แบบ seed จาก (วันที่ + id ทีม) ผลของรอบที่ผ่านไปแล้วจึงไม่เปลี่ยนเมื่อรีเฟรช
- ทีมยิ่ง OVR สูง โอกาสชนะต่อรอบยิ่งสูง

### รางวัลประจำวัน (จ่ายตอนข้าม 06:00)

| อันดับที่จบ | เหรียญ | แต้มแลกนักเตะ |
|---|---|---|
| 1 | 2,000,000 | 8,000 |
| 2 | 1,200,000 | 5,000 |
| 3 | 800,000 | 3,000 |
| 4–6 | 400,000 | 1,500 |
| 7 ลงไป | 150,000 | 500 |

ปรับได้ที่ `REWARD_TABLE` ใน `src/services/league.ts`

### ล็อกการเปลี่ยนตัว
- หลังกดเข้าร่วมลีก เปลี่ยนตัวได้ **ทุก 1 ชั่วโมง** (`SQUAD_LOCK_MS`)
- เข้าร่วมแล้วยังไม่เคยเปลี่ยน = เปลี่ยนได้ทันที 1 ครั้ง แล้วค่อยเริ่มนับคูลดาวน์
- ตรวจที่ `lockGuard()` ใน `useTeam` **จุดเดียว** ทุกทางที่แก้ทีมจึงโดนกติกาเดียวกัน
  (ลากวาง / เลือกจากหน้าต่าง / เอาออก / เปลี่ยนแผนการเล่น)
- หน้า MY TEAM มีป้าย 🔒 และแผงลีกบอกว่าเปลี่ยนได้อีกครั้งกี่โมง
- ออกจากลีกแล้วปลดล็อกทันที

### ผลย้อนหลัง
- ประวัติย้ายไปเก็บใน **บัญชี** แล้ว (เดิมอยู่ใน state ชั่วคราว หายทุกครั้งที่รีเฟรช)
  เก็บได้ 60 นัดล่าสุด (`HISTORY_LIMIT`)
- หน้า Match มีรายการผลย้อนหลัง กรองได้ ทั้งหมด / ลีก / กระชับมิตร
- กดแต่ละนัดเพื่อดูไทม์ไลน์ประตูรายนาที ใครยิง พลังทีมทั้งสองฝั่ง
  เหรียญที่ได้ และโอกาสชนะก่อนเริ่มแข่ง

### ความสัมพันธ์กับระบบเดิม
- นัดในลีกให้ **เหรียญ + คะแนนซีซัน** ตามปกติ ระบบซีซัน/rank จึงเดินหน้าไปด้วยกัน
- คะแนนลีก (3-1-0) แยกจากคะแนนซีซัน คนละตารางกัน:
  ลีก = แข่งกันรายวัน / ซีซัน = สะสม 14 วัน
- แมตช์กระชับมิตรได้เหรียญและคะแนนซีซัน แต่ **ไม่นับ** ในตารางประจำวัน

---

## แก้บั๊กที่เจอระหว่างทาง
- **ไม่มีเมนู Match ในแถบเมนู** — route `/match` มีอยู่และทำงานได้ แต่ไม่เคยถูกใส่ใน
  `NAV_ITEMS` เลยเข้าหน้านี้ไม่ได้ถ้าไม่พิมพ์ URL เอง (และ `getPageTitle` ก็ตกไปใช้ค่า
  สำรอง "My Team") เพิ่มเมนู **Match** ไว้ใต้ My Team แล้ว
- **ผลลีกหลายรอบถูกนับเข้าสถิติซีซันแค่รอบเดียว** — ตอนย้อนคำนวณหลายรอบติดกัน
  โค้ดอ่าน `record` จากค่าที่ React ยังไม่ได้ re-render ทำให้แต่ละรอบเขียนทับกันเอง
  แก้โดยเขียนค่าที่คิดได้กลับลง ref ทันที (แบบเดียวกับที่ทำกับสถานะลีก)
- **ไฟล์ซ้ำ `src/hooks/useMatchmaking.ts`** (ตัวโครงเปล่าเวอร์ชันเก่า) ถูกลบทิ้ง —
  ทั้ง TypeScript และ Vite เลือกนามสกุล `.ts` ก่อน `.tsx` เสมอ ระบบจับคู่แข่งขันตัวจริง
  จึงถูกทับด้วยของเก่าอยู่ (บั๊กนี้มีอยู่ก่อนแล้ว)
- **แต้มจากการย่อยการ์ดไม่เข้า** — โค้ดคิดแต้มอยู่ข้างใน updater ของ `setState`
  ซึ่ง React จะเรียกทีหลัง (และเรียกซ้ำใน StrictMode) ทำให้ได้ยอด 0 เสมอ
  แก้เป็นคิดจาก state ปัจจุบันก่อนสั่ง setState เรื่องเดียวกันนี้แก้ที่การบันทึกผลการแข่งด้วย
- **ซองเดียวราคา 1,200,000 แต่เงินเริ่มต้น 1,000,000** — ผู้เล่นใหม่เปิดซองไม่ได้เลย
  จึงเพิ่ม Starter Pack (50,000) และ Gold Pack (250,000) คู่กับ Legend Pack เดิม

## ยังไม่ได้ทำ (ถ้าอยากต่อ)
- ยังไม่มีการแจ้งเตือนตอนถึงรอบใหม่ขณะเปิดหน้าอื่นอยู่ (ต้องเข้าหน้า Match ถึงเห็น)
- ภารกิจ (Missions) ยังเป็น mock ล้วน — `progress` ฟิกซ์ไว้ตายตัว ไม่มีตัวนับ ไม่มีปุ่มรับรางวัล
- ตารางอันดับ **ซีซัน** ยังเป็นบอทที่คะแนนไม่ขยับ (ตารางลีกรายวันขยับแล้ว)
  พอผู้เล่นรีเซ็ตขึ้นซีซันใหม่ บอทควรรีเซ็ตด้วย
- ร้าน Exchange ยังโชว์นักเตะทั้ง pool ตลอดเวลา — ถ้าอยากให้ลุ้นขึ้น ทำเป็นหมุนเวียนรายวัน
  หรือล็อกการ์ดระดับสูงไว้ให้ผู้เล่นที่ถึงระดับ PLATINUM ขึ้นไปเท่านั้น
- นักเตะ BALE ใน roster ตั้งตำแหน่งเป็น GK อยู่ (น่าจะพิมพ์ผิด) — ยังไม่ได้แก้ให้ เพราะเป็นค่าเกม

---

## รอบล่าสุด — แก้ UI ยับตอนมีสกอร์ + เพิ่มการ์ดระดับ MYTHICAL

### A. แผง Matchmaking ไม่ดันเลย์เอาต์อีกแล้ว
**อาการเดิม:** กด matchmaking แล้วพอมีประตูเกิดขึ้น การ์ดในแถวแดชบอร์ดล่างจะสูงขึ้น
ทีละบรรทัดตามไทม์ไลน์ประตู ไปเบียดสนามด้านบน (ที่ล็อก `min-h-[460px]` ไว้) จนล้นทับกัน

**สาเหตุ:** `MatchmakingPanel` สูงตามเนื้อหา และเนื้อหาข้างในงอกได้ไม่จำกัด

**ที่แก้:**
- `src/components/matchmaking/MatchmakingPanel.tsx` — แบ่งแผงเป็น 3 ส่วนตายตัว
  หัว (ไม่ยืด) / ตัว (`flex-1 min-h-0 overflow-y-auto` เนื้อหาที่งอกถูกกักไว้ในนี้) / ท้าย (ปุ่ม)
  และล็อก `max-h-[340px] overflow-hidden` ในโหมด compact
  → ความสูงของแผงคงที่ ไม่ว่าจะยิงกี่ลูก
- ระหว่างถ่ายทอดสดในโหมดย่อ ซ่อนวง VS (แผงสดโชว์ชื่อสองทีม + สกอร์อยู่แล้ว เดิมซ้อนกันจนเบียด)
- `src/components/matchmaking/LiveMatchPanel.tsx` — ไทม์ไลน์ประตูล็อก `max-h-[84px]`
  (~3 บรรทัด) ประตูลูกใหม่เลื่อนเข้ามาแทนการดันแผงให้สูงขึ้น + กันตัวเลขสกอร์ถูกบีบตอนชื่อทีมยาว
- รายการประตูในหน้าจอผลการแข่ง ล็อก `max-h-24` เช่นกัน
- `src/components/layout/BottomDashboard.tsx` — เติม `shrink-0` ให้แถวนี้
- `src/pages/Match/MatchPage.tsx` — แผงจับคู่เป็น `sticky` ไม่เลื่อนหนีตอนอ่านผลย้อนหลังยาว ๆ

### B. การ์ดระดับ MYTHICAL (ระดับสูงสุด เหนือ legendary)
ลำดับระดับตอนนี้: `common → rare → epic → legendary → mythical`

| ไฟล์ | เปลี่ยนอะไร |
|---|---|
| `src/types/player.ts` | `Rarity` เพิ่ม `'mythical'` + export `RARITY_ORDER` |
| `tailwind.config.js` | สี `rarity.mythical` = `#FF3FA4` + keyframes `auroraSweep` / `shardOrbit` / `runePulse` |
| `src/components/pack/rarityFx.ts` | `RARITY_FX.mythical` (ซองสั่น 3200ms, spark 40, ember 34, layer `aurora`), `RARITY_RANK = 4` |
| `src/components/pack/PackFxLayers.tsx` | เลเยอร์ใหม่ `MythicAurora` — ม่านออโรราไล่สี + วงแหวนรูน 3 วง + คริสตัลโคจร 8 ชิ้น |
| `src/components/pack/PackRevealOverlay.tsx` | เรียกเลเยอร์ออโรราตั้งแต่จังหวะซองแตก |
| `src/services/sound.ts` | เสียง reveal ของ mythical: แฟนแฟร์ 2 ชั้น + เบสค้างยาว + เสียงอัฒจันทร์ดังกว่าเดิม |
| `src/services/salvage.ts` | เพดานแต้มแยกตามระดับ — mythical 9,000 (4 ระดับเดิมคงที่ 5,000 ไม่กระทบสมดุลเก่า) |
| `src/types/card.ts` + `src/data/cards.ts` | `PackTier` เพิ่ม `'mythic'` + ซองใหม่ **Mythic Pack** (1,200,000 เหรียญ, mythical 1%) |
| `src/components/pack/PackCard.tsx` | ซองแต่ละ tier มีสีของตัวเอง + ป้าย `MYTHICAL x%` บนซองที่มีลุ้น |
| `src/services/cardPack.ts` | `formatOdds` ตัด common ออก (บรรทัดยาวเกินช่อง) + เพิ่ม `getMythicalChance` |
| `src/data/roster.ts` | ยก 4 ใบท็อป (p061–p064) เป็น mythical, OVR 125 |
| `src/data/autoPlayer.ts` | ช่วง OVR อัตโนมัติของ mythical = 96–99 |
| `src/services/exchange.ts` | เพิ่มแท็บกรอง Mythical ในร้านแลก |
| `src/utils/helpers.ts` | `RARITY_STYLE.mythical` |
| `src/pages/Profile/ProfilePage.tsx` | ป้าย ★ เต็มเพดาน อ่านเพดานของระดับตัวเอง (ไม่ใช่ค่าคงที่ 5,000) |

**โอกาสออก mythical ต่อการ์ด 1 ใบ:** Starter 0% · Gold 0.1% · Legend 0.3% · Mythic Pack 1%

**อยากเพิ่มการ์ด mythical ใบอื่น:** แก้ `rarity` ในบรรทัดนั้นของ `src/data/roster.ts` เป็น `'mythical'`
ถ้าอยากให้ต้องเปิดซองเอาเท่านั้น เติม `owned: false` ต่อท้าย

---

## รอบล่าสุด (2) — เพิ่มเมนู Match + แยกการเปลี่ยนตัวออกจาก MY TEAM

### C. เมนู Match หายไปจาก Sidebar
`route /match` มีอยู่แล้วใน `App.tsx` แต่ไม่มีบรรทัดของมันใน `NAV_ITEMS`
เมนูซ้ายจึงไม่แสดง (เข้าได้ทางเดียวคือพิมพ์ URL เอง)

- `src/components/sidebar/navItems.ts` — เพิ่ม `{ id: 'match', path: '/match', label: 'Match', icon: '⚑' }`
  Sidebar กับ MobileNav อ่านจากไฟล์นี้ไฟล์เดียว จึงขึ้นพร้อมกันทั้งสองที่
  และ `getPageTitle` ก็จะโชว์ชื่อหน้าถูกต้องบน Header ด้วย

### D. แยก "เปลี่ยนตัว" ออกจาก MY TEAM (แก้ปัญหาโดนล็อกทีม)

**ปัญหาเดิม:** ทุกการแก้ทีมในหน้า MY TEAM เรียก `markSquadChange()` เหมือนกันหมด
พอเข้าร่วมลีกประจำวันแล้ว แค่ลากการ์ดย้ายตำแหน่งเล่น ๆ ก็โดนคูลดาวน์ 1 ชั่วโมงทันที
ทั้งที่รายชื่อ 11 คนไม่ได้เปลี่ยนเลย

**หลักการใหม่ — แยก "จัดตำแหน่ง" ออกจาก "เปลี่ยนตัว":**

| การกระทำ | นับเป็นเปลี่ยนตัว? | ติดคูลดาวน์ลีก? | ทำที่ไหน |
|---|---|---|---|
| สลับตำแหน่งของ 11 ตัวจริงกันเอง | ไม่ | **ไม่** | MY TEAM |
| เปลี่ยนแผนการเล่น (Formation) | ใช่ (จัดทีมใหม่ทั้งชุด) | ใช่ | MY TEAM |
| พาตัวสำรองขึ้นสนาม | ใช่ | ใช่ | Substitution |
| เอาคนออกจากสนาม | ใช่ | ใช่ | Substitution |

**ไฟล์ที่แก้:**
- `src/hooks/useTeam.tsx`
  - `swapSlots` ไม่เรียก `lockGuard` และไม่เรียก `markSquadChange` อีกแล้ว (คนชุดเดิม แค่ย้ายที่)
  - `assignCard` เช็คก่อนว่าการ์ดอยู่บนสนามอยู่แล้วหรือเปล่า
    ถ้าใช่ = สลับตำแหน่ง → ข้ามคูลดาวน์ / ถ้าไม่ใช่ = เปลี่ยนตัวจริง → ผ่านคูลดาวน์ตามปกติ
  - เพิ่ม `squadLock` ใน context (มี `remainingMs` / `nextChangeAt`) ไว้ทำนาฬิกานับถอยหลัง
- `src/components/pitch/FootballPitch.tsx` — เอาลิ้นชักตัวสำรองออกจากสนาม
  หน้าต่างเลือกนักเตะเหลือเฉพาะตัวจริงในช่องอื่น (สลับตำแหน่ง) และมีปุ่มลัดไปหน้า Substitution
- `src/components/pitch/SlotPickerModal.tsx` — เพิ่ม prop `mode="positional"` (ซ่อนปุ่ม "เอาออก"
  และเปลี่ยนข้อความให้ตรงกับหน้าที่ใหม่) · `onClear` กลายเป็น optional
- `src/pages/Substitution/SubstitutionPage.tsx` (**ไฟล์ใหม่**) — หน้าเปลี่ยนตัวเต็มรูปแบบ
  เลือกตัวออกจาก 11 ตัวจริง → เลือกตัวเข้าจากม้านั่ง (กรองตามกลุ่มตำแหน่งได้) → ยืนยัน
  โชว์ค่าพลังจริงเมื่อลงช่องนั้น ปุ่มจางอัตโนมัติถ้าชื่อซ้ำในทีม
  และมีนาฬิกานับถอยหลังคูลดาวน์ของลีกแบบเดินจริงทุกวินาที
- `src/components/sidebar/navItems.ts` + `src/App.tsx` — เมนู/route `Substitution`

**หมายเหตุ:** `src/components/pitch/SubsDrawer.tsx` ไม่ได้ถูกใช้แล้ว ลบทิ้งได้ถ้าไม่ต้องการเก็บไว้

**ถ้าอยากให้ MY TEAM ยังพาตัวสำรองขึ้นสนามได้เหมือนเดิม:** ใน `FootballPitch.tsx`
เอาตัวสำรองกลับเข้า `pickerCandidates` และเปลี่ยน `mode="positional"` เป็น `mode="full"`
กติกาคูลดาวน์ใน `useTeam.tsx` จะยังทำงานถูกต้องอยู่ (ล็อกเฉพาะตอนพาคนใหม่ขึ้นสนาม)

---

## รอบล่าสุด (3) — ยกเลิกคูลดาวน์ / คลิกการ์ดสลับตำแหน่ง / รางวัลในหน้า MATCH / ดูนักเตะในซอง

### E. ยกเลิกหน้า Substitution — กลับมารวมที่ MY TEAM
- ลบ `src/pages/Substitution/` ออก พร้อม route และเมนู
- ลิ้นชักตัวสำรอง (`SubsDrawer`) กลับมาอยู่บนสนามเหมือนเดิม
- **คลิกการ์ดในสนาม 1 ครั้ง = เด้งรายชื่อการ์ดในคลัง** ของตำแหน่งนั้นขึ้นมาทันที
  (แบบเดิมที่เคยมี) กดเลือกใบไหนก็สลับลงได้เลย
  หน้าต่างมี 2 แท็บ: "เล่นตำแหน่งนี้ได้" (ค่าเริ่มต้น) และ "ทั้งหมด"
  รายชื่อรวมทั้งตัวสำรองในคลังและตัวจริงในช่องอื่น (เลือกตัวจริงคนอื่น = สลับตำแหน่งกัน)
  เรียงให้ตรงตำแหน่งมาก่อน แล้วเรียงตามค่าพลังจริงในช่องนั้น
  คนที่ชื่อซ้ำกับคนที่ลงสนามอยู่แล้วเป็นการ์ดขาวดำ กดไม่ได้ และถูกดันไปท้ายรายการ
  มีปุ่ม "เอาออก" สำหรับเคลียร์ช่องนั้น
- ยังลากวางได้เหมือนเดิม และคลิกการ์ดในลิ้นชักตัวสำรองก่อนแล้วค่อยจิ้มช่องก็ได้

### F. ตัดคูลดาวน์เปลี่ยนตัวออกทั้งหมด — MATCH ยึดทีมจาก MY TEAM
ทั้งลีกประจำวันและแมตช์กระชับมิตรใช้ทีมชุดล่าสุดของหน้า MY TEAM เสมอ แก้เมื่อไหร่มีผลทันที
- `src/hooks/useTeam.tsx` — ลบ `lockGuard`, `markSquadChange`, `squadLocked`, `squadLock` ออกหมด
- `src/services/league.ts` — ลบ `SQUAD_LOCK_MS`, `SquadLock`, `getSquadLock`
- `src/hooks/useLeague.tsx` + `src/components/league/LeaguePanel.tsx` — ตัด UI ล็อกทีมออก
  เปลี่ยนเป็นข้อความยืนยันว่าใช้ทีมชุดล่าสุดจาก MY TEAM
- ฟิลด์ `lastSquadChangeAt` ยังอยู่ในโครงบัญชีที่เซฟไว้ (ค่าเป็น null เสมอ) เพื่อไม่ให้บัญชีเก่าพัง

### G. หน้า MATCH — คอลัมน์ขวาเปลี่ยนเป็นรายละเอียดรางวัล
- `src/components/league/RewardsPanel.tsx` (**ไฟล์ใหม่**) แทนที่แผง Matchmaking เดิม ประกอบด้วย
  1. รางวัลที่จะได้ถ้าจบวันนี้ด้วยอันดับปัจจุบัน (เหรียญ / แต้มแลกนักเตะ / แต้มตีบวก)
  2. ตารางรางวัลปลายวันของอันดับ 1, 2, 3, ≤5, ≤10 — แถวของอันดับตัวเองไฮไลต์ให้
  3. คะแนนลีกต่อนัด (3-1-0) และกติกาตัดสินเมื่อคะแนนเท่ากัน
  4. รางวัลรายนัดของแมตช์กระชับมิตร (ชนะ/เสมอ/แพ้ + แต้มตีบวก)
  ตัวเลขทั้งหมดอ่านจาก `services/league.ts` โดยตรง ปรับสมดุลที่ไฟล์เดียวแล้วแผงนี้เปลี่ยนตาม
- `src/components/matchmaking/MatchLiveOverlay.tsx` (**ไฟล์ใหม่**) — แผงจับคู่ย้ายมาเป็นหน้าต่างลอย
  โผล่เฉพาะตอนมีแมตช์กระชับมิตรค้างอยู่ (เจอคู่/กำลังแข่ง/มีผล) กดท้าทีมแล้วยังดูถ่ายทอดสดได้ครบ
- แผง Matchmaking แบบย่อบนแดชบอร์ดล่างของ MY TEAM ยังอยู่เหมือนเดิม

### H. ร้าน CARD PACK — ใส่นักเตะเองได้ + โชว์ใบเด่น + กดดูของในซอง
- `src/types/card.ts` — `CardPack` เพิ่มฟิลด์ `pool?: string[]`
  ใส่ id นักเตะที่อยากให้อยู่ในซองนั้น (ดู id ได้ที่ `src/data/roster.ts`)
  ไม่ใส่ = สุ่มจากนักเตะทั้งเกมตาม odds เหมือนเดิม
- `src/services/cardPack.ts` — เพิ่ม `getPackPlayers` / `getPackHighlight` /
  `getPackPlayersByRarity` / `getPackRarities` · `openPack` สุ่มจาก pool ของซองแล้ว
  ใบเด่น = คน OVR สูงสุดในซอง ถ้าเสมอกันเลือกด้วยชื่อซองเป็น seed (คงที่ ไม่เปลี่ยนตอนรีเฟรช
  และซองแต่ละใบในร้านโชว์คนละหน้า)
- `src/components/pack/PackCard.tsx` — หน้าซองโชว์การ์ดใบเด่นจริง ๆ พร้อมชื่อ/ระดับ/OVR
  แทนกล่อง ⬢ เดิม · กดที่การ์ดหรือปุ่ม "ดูนักเตะในซอง (N คน)" เพื่อเปิดรายชื่อ
- `src/components/pack/PackContentsModal.tsx` (**ไฟล์ใหม่**) — รายชื่อทุกคนที่ซองมีโอกาสออก
  จัดกลุ่มตามระดับ (ดีสุดอยู่บน) พร้อมโอกาสของแต่ละระดับและตัวกรอง
- `src/data/cards.ts` — เพิ่มซองตัวอย่าง **Icon Pack** (600,000 เหรียญ) ที่ใช้ `pool`
  คัดนักเตะไว้ 12 คน ออกได้เฉพาะรายชื่อนี้เท่านั้น
- `src/pages/CardPack/CardPackPage.tsx` — ปรับกริดรองรับ 5 ซอง

**วิธีทำซองคัดนักเตะเอง:** เพิ่ม `pool: ['p021', 'p037', ...]` ในซองนั้นที่ `src/data/cards.ts`
ระบบจะหยิบใบ OVR สูงสุดในรายชื่อไปโชว์หน้าซองให้อัตโนมัติ
