# ทำให้เล่นออนไลน์จริงบน Vercel — คู่มือทีละขั้น

เกมนี้ตอนนี้มี **สองโหมด** และสลับให้เองอัตโนมัติ:

| | โหมดออฟไลน์ (ไม่ตั้งค่าอะไรเลย) | โหมดออนไลน์ (ตั้งค่า Firebase แล้ว) |
|---|---|---|
| บัญชี | เก็บใน localStorage ของเครื่องนั้น | Firebase Auth — ล็อกอินได้ทุกเครื่อง |
| เซฟ (เหรียญ/การ์ด/ทีม) | อยู่ในเครื่อง ล้างแคชแล้วหาย | Firestore ตามบัญชีไปทุกที่ |
| ตารางอันดับ | ทีมจำลอง | **ผู้เล่นจริงทั้งเซิร์ฟเวอร์ อัปเดตสด** |
| จับคู่แข่งขัน | บอทสุ่ม | **ทีมของผู้เล่นจริงที่พลังใกล้เคียงกัน** |

ทั้งหมดใช้เวลาประมาณ 15 นาที และฟรีทั้งคู่ (Firebase Spark + Vercel Hobby)

---

## ขั้นที่ 1 — สร้างโปรเจกต์ Firebase

1. เข้า <https://console.firebase.google.com> → **Add project** → ตั้งชื่อ เช่น `apex-eleven` (ปิด Google Analytics ได้ ไม่จำเป็น)
2. เมนูซ้าย **Build → Authentication** → Get started → แท็บ **Sign-in method** → เปิด **Email/Password** → Save
3. เมนูซ้าย **Build → Firestore Database** → Create database → เลือก **Production mode** → โซนแนะนำ `asia-southeast1` (สิงคโปร์ ใกล้ไทยที่สุด)
4. หน้า Firestore → แท็บ **Rules** → ลบของเดิมทิ้ง → วางเนื้อหาจากไฟล์ `firestore.rules` ในโปรเจกต์นี้ → **Publish**

> กฎในไฟล์นั้นสำคัญมาก: มันกันไม่ให้ใครแก้คะแนนหรือเปิดดูคลังการ์ดของคนอื่นได้

## ขั้นที่ 2 — เอาค่าคอนฟิกมาใส่

1. ไอคอนเฟือง ⚙ → **Project settings** → เลื่อนลงไปหัวข้อ **Your apps** → กดไอคอน `</>` (Web)
2. ตั้งชื่อ app อะไรก็ได้ → Register app (ไม่ต้องติ๊ก Firebase Hosting เพราะเราจะ deploy ที่ Vercel)
3. จะได้ค่าชุดหนึ่งหน้าตาแบบนี้ `apiKey`, `authDomain`, `projectId`, ...
4. ในเครื่องตัวเอง: คัดลอก `.env.example` เป็น `.env` แล้วเติมค่าลงไป

```bash
cp .env.example .env
npm install
npm run dev
```

เปิดเกมแล้วดูมุมซ้ายล่างของหน้าล็อกอิน ถ้าขึ้นจุดเขียว **"บัญชีออนไลน์ · เล่นต่อได้ทุกเครื่อง"** แปลว่าต่อติดแล้ว

## ขั้นที่ 3 — ขึ้น Vercel

**วิธีที่ 1: ผ่าน GitHub (แนะนำ — แก้โค้ดแล้ว deploy ใหม่อัตโนมัติ)**

```bash
git init
git add .
git commit -m "APEX ELEVEN online"
git remote add origin https://github.com/<ชื่อคุณ>/apex-eleven.git
git push -u origin main
```

จากนั้นที่ <https://vercel.com/new> → Import repository นี้ → Vercel จะอ่าน `vercel.json` แล้วตั้งค่าให้เองทั้งหมด

**วิธีที่ 2: ผ่าน CLI (ไม่ต้องมี GitHub)**

```bash
npm i -g vercel
vercel        # ครั้งแรก: ตอบคำถามตั้งค่าโปรเจกต์
vercel --prod # ขึ้นเวอร์ชันจริง
```

## ขั้นที่ 4 — ใส่ค่า env บน Vercel (ข้ามไม่ได้)

ไฟล์ `.env` ไม่ถูกอัปขึ้น git ดังนั้นต้องกรอกบน Vercel อีกรอบ:

**Vercel → โปรเจกต์ → Settings → Environment Variables** ใส่ทั้ง 6 ตัว
(ติ๊กให้ครบทั้ง Production / Preview / Development)

```
VITE_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN
VITE_FIREBASE_PROJECT_ID
VITE_FIREBASE_STORAGE_BUCKET
VITE_FIREBASE_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID
```

> ⚠️ ใส่ค่าแล้วต้องกด **Redeploy** หนึ่งครั้ง เพราะ Vite ฝังค่าพวกนี้ตอน build
> ถ้าลืม เกมจะขึ้นเว็บได้แต่กลายเป็นโหมดออฟไลน์

## ขั้นที่ 5 — อนุญาตโดเมนของ Vercel

Firebase Console → **Authentication → Settings → Authorized domains** → **Add domain**
ใส่โดเมนที่ Vercel ให้มา เช่น `apex-eleven.vercel.app` (ถ้ามีโดเมนของตัวเองก็ใส่เพิ่ม)

**ถ้าลืมขั้นนี้ จะสมัคร/ล็อกอินบนเว็บจริงไม่ได้** ทั้งที่ในเครื่องตัวเองใช้ได้ปกติ

---

## ทดสอบว่าออนไลน์จริงไหม

1. เปิดเว็บบนคอม สมัครไอดี `manager-a`
2. เปิดเว็บเดียวกันบนมือถือ (หรือหน้าต่างไม่ระบุตัวตน) สมัคร `manager-b`
3. เล่นให้แต่ละไอดีชนะสักนัด → เข้าหน้า **ตารางอันดับ**
   ต้องเห็นชื่อทีมของอีกเครื่องโผล่มาในตาราง พร้อมข้อความ "อันดับสดจากผู้เล่นจริง N คน"
4. กดหาคู่ในหน้า **Match** หลาย ๆ ครั้ง จะเจอชื่อทีมของผู้เล่นจริงปนมากับบอท

---

## รู้ไว้จะได้ไม่งง

- **การแข่งเป็นแบบ "เจอทีมจริง แต่ไม่ต้องรอคนอื่นออนไลน์"** — ระบบดึงทีมล่าสุดของผู้เล่นคนนั้น (ค่าพลัง/แผน/ชื่อ) มาเป็นคู่แข่งแล้วจำลองผลทันที
- **ผลตกถึงทั้งสองฝั่ง** — ผู้ถูกท้าจะได้รับใบรายงานผลลง `matchInbox` ของตัวเอง พอเปิดเกมครั้งถัดไปคะแนน/สถิติ/ประวัติจะอัปเดตให้เอง พร้อมแจ้งเตือน "ทีมของคุณถูกท้า" (ฝ่ายตั้งรับได้เหรียญน้อยกว่า เพราะไม่ได้ลงมือเล่นเอง)
- **เซฟขึ้นคลาวด์แบบหน่วง 2.5 วินาที** หลังหยุดเล่น เพื่อไม่ให้เปลืองโควตา Firestore และจะบังคับเซฟทันทีตอนปิดแท็บ/ออกจากระบบ
- **ตารางอันดับดึงมา 100 อันดับแรก** ถ้าผู้เล่นจริงยังไม่ถึง 12 คน จะเติมทีมจำลองให้ตารางไม่โล่ง แล้วทีมจำลองจะถูกเบียดออกเองเมื่อคนเยอะขึ้น
- **รีเซ็ตรหัสผ่านทางอีเมลยังทำไม่ได้** เพราะระบบแปลงไอดีผู้เล่นเป็นอีเมลภายใน (`cloudAccount.ts`) ถ้าต้องการ ให้เปลี่ยนหน้าสมัครไปรับอีเมลจริงแทน
- **ค่า `VITE_FIREBASE_*` เปิดเผยได้** ไม่ใช่ความลับ ความปลอดภัยทั้งหมดอยู่ที่ `firestore.rules`

## เจอปัญหาบ่อย ๆ

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| กดสมัครแล้วขึ้น "ยังไม่ได้เปิดวิธีล็อกอินแบบ Email/Password" | ทำขั้นที่ 1 ข้อ 2 ให้ครบ |
| ขึ้น "เชื่อมต่อเซิร์ฟเวอร์ไม่สำเร็จ" เฉพาะบนเว็บจริง | ยังไม่ได้เพิ่มโดเมน Vercel ใน Authorized domains (ขั้นที่ 5) |
| หน้าล็อกอินยังขึ้น "โหมดออฟไลน์" ทั้งที่ใส่ค่าแล้ว | ยังไม่ได้ Redeploy หลังใส่ env / ชื่อตัวแปรสะกดผิด (ต้องขึ้นต้น `VITE_`) |
| ขึ้น "สิทธิ์ไม่พอ" | ยังไม่ได้ Publish `firestore.rules` |
| เข้า `/leaderboard` ตรง ๆ แล้ว 404 | ต้องมีไฟล์ `vercel.json` (มีให้แล้ว) — ถ้าตั้งค่า deploy เองอย่าลืม rewrite ทุก path ไป `index.html` |
